# 上游功能语义适配

上一阶段基线：`message_feature@ec870c1f`。2026-09-10 本轮基线：`message_feature@c03f9c4da45c296191e53fb014f4c07e3753e793`；开始时工作区干净，原 stash `7a6ef9155ccd8e5ce92768702da03b5ae8d7184b` 保留。按明确要求继续当前分支，语义移植，不 cherry-pick、不覆盖上游整文件、不 commit/push。开发验证使用 `pnpm dev`，不执行 build。

更新：2026-09-10。前面的阶段记录保留历史结果；本轮范围、实现与验证见本文末尾。

## 上一阶段实施与验收进度

- [x] 播放器模式确认、结束推荐、随机播放归属、尺寸调度与竖屏控件。
- [x] PGC 原位播放器、剧集信息、原生 React 交互及切集生命周期。
- [x] 关系写入闭环与在途动态转发事务。
- [x] 转发视频身份、预览设置/拖动复用、交互保留及动态窗口去重。
- [x] MAIN 缺失父评论；本地动态评论的已有等价实现保留。
- [x] 关注分组读取、管理与列表交互。
- [x] 首页 URL、缓存入口置顶、最小启动脚本。
- [x] ContextMenu 行为、元素设置菜单与可选推荐模式胶囊。
- [x] 追加修复原版右栏覆盖画面、播放页刷新暴露原版布局的问题。
- [x] 私信左右边线、仅入场展开/切换淡出、历史逐条浮现与输入栏材质轮廓。
- [x] 当前源码完整 lint、typecheck、test、knip 与开发编译。
- [ ] 最新构建的完整实站桌面 QA；受控性能计数已完成，实站矩阵见下文。

## 上游条目

完整 diff 保存在本机临时审查目录 `/tmp/bewly-upstream-20260908/`；只作为参考，不在运行时使用。上游跟踪引用为 `f8e18fec`，本地包含 Nocturne 独立定制，不向 upstream 推送。

| 提交 | 对应职责 | 状态 |
|---|---|---|
| f39494a / 830793c | 模式应用资格、结束推荐、生命周期 | 已适配；以原生模式状态或本地稳定布局确认完成，点击不记成功；结束推荐不重进、不广播补偿 resize |
| 387666a768b567a167e386f1679c184759b30694 | 调整竖屏视频放大控件位置 | 已按本地原位播放器、尺寸观察与 RAF 定位适配；旧记录将该提交写成“时钟相关生命周期”有误，现按原始提交主题纠正 |
| 200c40e | 播放器、随机播放、URL 和菜单一致性 | 已适配；保留 Nocturne 的辅助按钮入口及原生播放器原位锚定 |
| a5a61e8 | PGC 布局与原生交互 | 已适配；另按实站补齐 PaginatedEpList 完整目录、分页/季切换/排序所属根，不复制搬运播放器的 CSS 假设 |
| 143068e + 566a3fb | 关注分组最终功能 | 已适配普通、默认、特别关注、创建/重命名/删除/移动；悄悄关注的入口、专用 API、类型和文案不引入 |
| 89e4aac / 1554f35 | 关注列表操作、语义和行布局 | 已适配；按钮语义、单/双行居中；删除批量离场绝对定位，保留原有视频分页和选择状态 |
| 801c6ed | 视频卡菜单与可见项目分组 | 已适配 Web/App 实际分组、无空分隔、不可压缩的分隔线；已关注作者隐藏拉黑的上游策略不引入 |
| 9fc254f + dd0466b | 转发引用身份、视频布局、悬停预览 | 已适配；区分转发者/原动态作者/视频作者，实际 bvid/cid 与分 P；额外延迟默认关闭，保留当前立即触发策略 |
| 4f1febb | 动态虚拟窗口 | 已适配等价窗口复用与 ref 去重；保留 CardRowMetrics / momentColumnIndex，并补交互保留与不连续间距 |
| 547267d | 动态“更多回复”及树线 | 本地等价保留：useMomentCommentThread 按服务端分页和去重结果判断，MomentCommentTreeGuides 绘制 SVG 路径；未替换成本地不使用的上游 Comments 组件 |
| f8e18fe | 原生评论缺失父链 | 已适配 MAIN 的共享缺失父占位、已知链/排序/折叠/测量及真实节点替换；未知不等于删除，不编造作者正文 |
| ef5d48a | 首页缓存复制成本 | 所有权转移、推荐历史浅复制为本地等价保留；补过期 generation 在计算 snapshot getter 前退出，不引入递归克隆 |
| 3b1548c | 重型包之前的启动处理 | 已适配独立 document-start 小入口、单一启动清理与超时原站恢复；不复制全部初始化立即执行的策略，不使用 body display:none |
| 8952da6 | 元素设置右键菜单 | 已适配现有 ContextMenu、布局编辑注册表、设置导航和配置真值；不恢复旧顶栏模式切换器 |
| 3967caa | 可选推荐模式胶囊 | 已适配，默认关闭；使用现有推荐设置、App 授权、缓存分区、布局编辑和四语言，独立于三档页面模式 |

## 修改边界和状态归属

- `playerModeApplication.ts` 只确认当前任务；`playerMedia.ts` 统一媒体/原位根身份。`index.ts` 沿用导航、Drawer、可见性和重试生命周期，确认后才写完成标记及伴随设置。`playerDomLifecycle.ts`、`randomPlay.ts` 与 `verticalVideoZoom.ts` 共用发现机制；旧 video 仍连接不阻止跟随新主播放器。
- 播放页适配留在现有 `bewlyWidescreen/` 的 constants/nativeDom/videoInfo/geometry/nativeControls/sidebar/playlist/styles；PGC 的 mediaInfo、工具栏和完整目录保留原生节点。MAIN 的 `movedPgcReact.ts` 只为已标记、确实离开原委托根的已挂载节点转发事件，包含点击、悬停、鼠标/指针长按序列；重复绑定、嵌套边界和释放均有明确归属。
- 关注目录为 `Home/following/useFollowingDirectory.ts` 的唯一写入拥有者；分组写入由 `useFollowingGroupWrites.ts` 固定提交快照，菜单/表单与列表展示分别放在 `FollowingGroupActions.vue` / `FollowingSidebar.vue`。读取失败保留现有目录；移动后关闭菜单，重开权威查询成员分组。原有纯视频旧布局保留，分组用于带 UP 侧栏的新布局。
- `userRelation.ts` 统一成功关系通知；原有关系读取 composable 移到共享位置，按账号持有唯一只读关系集合。查询失效与账号失效分开，取消一次搜索不会清空其他卡片的确认结果；写入中的按钮状态归发起者，不再混进关系真值。
- `momentForwardTransactions.ts` 复用原有内存草稿预算、区分 draft revision，并由页面/账号处理已经发出的请求。卡片卸载不等于服务端取消；成功不恢复旧草稿、不自动重发，也不清除更新后的草稿。
- `MomentVideoPreview.vue` 只负责媒体元素、首帧占位与交互；连接仍属于 `useMomentPreviews` / `createPreviewMediaSession`。`useVideoPreviewSwipeSeek.ts` 由 VideoCard 和动态卡共同使用。换源、重入和账号更替沿用媒体 generation；取消的拖动不会变成视频跳转。控制条/拖动默认关闭、封面触发默认开启。
- 虚拟窗口只保留焦点、编辑浮层、在途操作、拖动及全屏卡片，释放后及时回收。普通展开不形成永久保留；缓存恢复的转发编辑区不自动抢焦点。成员对象替换、实际高度/字号/宽度变化仍会失效索引和占位。
- `commentMissingParents.ts`（utils）负责纯父链补全，`inject/commentMissingParents.ts` 负责占位 DOM 与测量边界；MAIN 沿用既有评论 identity/epoch、分页和 Observer。账号变化通过现有页面桥清理，没有另建评论控制器。被替代的原生旧排序、引用补丁及辅助分支已删除。
- `useHomePageRoute.ts` / `homeRoute.ts` 统一 page/tab 解析与写回，复用 `useCurrentLocationHref`。所有写回保留 history.state，延迟清理检查当前 URL；首页入口置顶和内部标签滚动恢复仍由原有 Home/网格协议协调。
- `pageLoading.ts` / `pageLoadingGuard.ts` 在主包前执行，接入 manifest 与现有 tsup 入口。首页与播放页隐藏保留布局测量，iframe 按自身范围揭示；Vue shell 挂载不再过早结束播放页遮挡。播放模式确认、用户退出、非 Bewly 模式和失败超时回到同一启动清理入口。
- 菜单显式区分 action/toggle/radio 及 closeOnSelect；操作打开 Dialog/设置后不抢回焦点。元素设置菜单复用 `layoutEdit` 描述和 settingsStore；Dock 可见性沿用同一修改方法。推荐胶囊通过 `recommendationMode.ts` 与设置页共用选项及授权动作。

## 2026-09-09 追加的播放页回归

实站 `BV1Zibx6yExR` 在较早开发构建 `mtsoyvnl` 下确认：原版 `.left-container.scroll-sticky` 仍是 `position: sticky`，形成独立层叠上下文，即使内部播放器 z-index 很高，后面的原版右栏仍可盖在视频上。原位祖先只清除 z-index/transform 并不足够。

修复将已标记的原位祖先设为 static；原版 `.right-container` / PGC `.plp-r` 中尚未移交的模块仅隐藏绘制，保留原 DOM、尺寸和业务所有权，移到 Bewly 侧栏后自动离开隐藏范围。退出模式撤掉类和规则即恢复原站。刷新时的早期隐藏在播放页延续到模式/控件布局确认，不再在 Vue shell 刚挂载时放开原版页面。

## 2026-09-09 私信交互修复（已提交阶段 05215cea）

- 根因：右侧改为透明表面时同时移除了边框；旧展开 reducer 仍根据滚动、回到最新消息与分页完成改变容器形态。切换会话直接替换组件，没有旧内容的离场阶段；输入栏使用普通面板的 12px 圆角，与播放栏的 modal 轮廓不一致。
- `ConversationView.vue` 恢复左右各 1px 语义边线，保持透明背景和已有上下滚动边缘模糊。展开状态只由会话进入/释放控制，滚轮、键盘、历史分页与“新消息”按钮只维护阅读位置和已读资格。删除 `conversationExpansion.ts` 中替代后的滚动收起 reducer、进度和圆角测量分支，保留视口几何与阅读意图纯函数。
- `WhisperWorkspace.vue` 使用现有 Vue Transition 顺序淡出旧会话，再挂载最终选择的会话；旧 DOM 淡出时组件已释放并设为 inert。新会话沿用 200ms 上下展开，完成后才发历史请求，再按实际可见消息逐条浮现。快速切换跳过未挂载的中间项；账号变化立即清除旧内容；焦点等待新 ref，关闭仍回到原会话列表项。临时收件人被确认成正式会话时保留同一组件，避免重置输入和阅读状态。
- 消息输入栏复用播放页底部容器的 `--bew-modal-radius`、平滑圆角、边框、边缘阴影及零 inset 液态边框；内部文本框按现有 8px 内距使用 16px 圆角、普通实色表面。继续使用同一 `vLiquidGlass` 设置和释放路径，没有新增玻璃气泡或内部模糊。
- 实际 ConversationView + 读取 controller 测试覆盖展开前零请求、三条消息的递增延迟、末条浮现后 ACK、滚轮/Home/End/最新消息按钮、分页在滚回底部后返回、迟到响应与卸载/换号。实际 Workspace + Vue Transition 测试覆盖旧组件先清理、顺序离场、快速选择、焦点恢复、跨账号立即清除及临时收件人确认复用。输入法、已购/收藏表情、图片选择与明确发送的原有组件测试保留。

## 2026-09-09 后续界面修订（基线 05215cea）

上一阶段全部修改已按用户要求提交并推送到 `origin/message_feature@05215cea`。以下为同分支的新一轮修改，不自动提交或推送。

- 私信切换范围从整个 ConversationView 收窄为内部历史记录：同一账号下用户切换保留外框、滚动容器、输入栏及液态挂载；仅旧记录淡出后请求最终选中的用户，再逐条浮现。初次打开仍等待上下展开完成，切换时不重复展开。旧请求通过现有 activation/layout generation 失效，滚动位置写回固定的前一用户；初始加载或正在切换时不把旧位置写到新用户。
- 输入栏 `bottom: 0` 对齐同高的左侧用户栏底部，保持已统一的 modal 圆角与材质。持久的 MessageComposer 按会话 key 更新原有草稿，清理表情弹层、输入法临时状态和图片错误；文件选择及表情插入的迟到回调仍归原会话，不向新用户附加旧选择或抢焦点。
- 去掉右上角关闭按钮，再次点击当前已选用户关闭详情并恢复列表焦点，按钮提供 aria-expanded。页面直接清除当前会话 URL，保留其他 history.state；删除只为旧关闭回退行为服务的三个 history marker 工具，避免 A→B 后“关闭 B”却重新打开 A。
- Reply / At / Love 的原有演员、互动正文与时间放在左侧，引用、封面/标题和来源链接放在右侧。真实卡片与骨架使用同一列定义；长标题可显示三行，图片失败仍保留标题和链接，缺少来源元数据继续保留原版入口。System 的布局和四类 Feed 的分页、读取与已读协议保持原有实现。
- 搜索框外壳恢复普通毛玻璃背景与共享模糊 token，输入元素透明且不再承担第二层背景。液态开启时由既有 data 属性替代普通模糊，关闭后自动恢复；全局禁用毛玻璃仍通过统一 token 使用实色/无滤镜。未修改顶栏渐进模糊。
- AppAuthorizationDialog 使用现成 `showTopBlur=false`，只移除该授权窗口遮到说明文字的顶部局部模糊，二维码、说明、刷新、轮询、焦点和普通 Dialog 毛玻璃保留。
- 新增 `verify-notification-ui.mjs` 并注册到现有 selected-p2；以实际组件和页内函数覆盖三类通知的左右内容与链接/图片回退、骨架列、关闭后保持列表 URL、授权窗口无顶部模糊及轮询清理。既有私信测试改为验证同一 shell/composer/textarea 节点、各会话草稿、文件选择归属、记录离场期间跳过中间请求和换号/卸载；搜索材质使用实际 attachment 与编译 CSSOM 检查单一模糊路径和观察器释放。

## 2026-09-09 阶段验证（历史）

该阶段完整运行结果：`pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm knip` 退出码均为 **0**。`git diff --check` 退出 0；以下计数与构建标记属于该历史阶段，不代替 2026-09-10 的验证。

- `pnpm test` 包含 targeted-fixes、selected-p2、private-message、notifications 四组完整脚本；包含后续界面修订的最终日志有 294 项 PASS 输出，另包含定向组合脚本通过信息。保留原有行为断言，因职责移动和明确取消滚动收起/整个会话淡出更新测试依赖、调用入口与行为期望；不是只检查新增源码字符串。
- 新增八份 `verify-upstream-*.mjs` 由既有 selected-p2 注册。使用实际模块/组件、可控 Promise 和原生结构 fixture，覆盖模式点击未确认、旧媒体、换号、卸载、重开分组菜单、写失败、转发重挂载/新草稿、全屏保留、分 P 媒体、首帧、缺失父占位、目录替换、首页 URL/历史状态、缓存 getter、菜单焦点与启动交接。
- 几何计数：100 次相同布局请求为 0 次新增原站 resize；一次真实尺寸变化为 1 次；结束推荐期间不广播，重放后补 1 次。PGC 没有 BV 在线人数节点仍可由实际控制/发送栏稳定确认。
- 动态计数：同一组 3000 条、同一两列布局，100 次等价窗口为 0 次重复赋值，100 次重复 ref 为 0 次新增观察器注册；测试窗口实际挂载 42 张卡片，并验证不连续交互保留。
- 竖屏控件计数：100 次普通 pointermove 为 0 次布局读取，100 次尺寸信号合并为 1 次 RAF；隐藏缩略导航不调度画面绘制。以上均为实际模块的受控数据与布局，不是实站长任务或收益百分比。
- knip 仍报告 **51 个未使用导出、6 个导出类型、4 个枚举成员**，包含既有后台 transport 导出和历史播放器工具等；退出 0 不代表没有提示，也未通过关闭检查隐藏它们。没有据此宣称全仓不存在死代码。
- 已重启当前项目 `pnpm dev`，MAIN、内容脚本、后台及新增首屏入口均完整编译成功；最新构建标记 **mtsr4vh6**。独立首屏脚本约 2.32 KB；未执行生产 build，也未修改其他 worktree 的开发进程。

## 实站与剩余验收

- 实站结构已核对 `/bangumi/play/ss2572` 与国创 `/bangumi/play/ep1231540`：原位播放器包装/比例占位、Nano 控制栏/发送栏、mediaInfo/工具栏及现代 PaginatedEpList。国创样本实际 video readyState=4，ss2572 有地区限制，未绕过。
- `mtsoyvnl` 下已定位用户截图所示的真实原版右栏叠层问题；刷新后检查样式确认该页面仍未加载追加修复，未把旧脚本现象当作新修复结论。
- **mtsr4vh6 的实站验收待扩展重载**：重载请求已发出。需继续检查两张截图对应 BV、可播放 PGC 的连续切集/目录排序、右栏打开与退出恢复、原生投币/收藏/分享弹窗、三主题、桌面窄窗及缩放、菜单和首页切标签。真实账号关注/分组写入及转发发送未作为实站测试执行。
- 本轮新开的私信验收页实际运行 `mtsr4vh6` 的较早内容脚本：`data-expansion-state="history-open"`，左右边框 0px，输入栏圆角 12px；确认仍是本轮修改之前的版本。当前 dev 已增量编译成功；已请求重新加载开发扩展，随后需核对 `expanded` 标记、左右 1px 边框、24px 外壳、切换/滚动/焦点与三主题桌面表现。未将旧页面现象或组件 fixture 写成新版实站通过。
- 后续界面修订验收再次刷新了同一独立测试标签页：已加载上一阶段的 `expanded`，但仍有右上角关闭按钮、缺少新 `.conversation-view__history`，输入栏与左侧底部相差 16px。已确认 Chrome 仍缓存后续修订前的脚本，并发出本轮重载请求；待验证底部对齐、仅记录切换、重复点击关闭、三类通知的桌面宽窄窗/缩放与明暗/OLED，以及关闭液态后的搜索框、授权二维码说明区。未进行真实私信发送或扫码授权。
- 刷新过程尚未做逐帧视觉记录，实站长任务/资源存活未完成同条件对比；不将 fixture 几何计数或开发编译描述为实站视觉/性能通过。上述源码和受控回归完成状态不等同于未执行的浏览器矩阵。

## 2026-09-10：dd0466bb..e8989942 最终语义适配

完整范围为 `dd0466bb9b22c1ea66700207765584bc4daadb3c..e89899422e6920dad9a67fafdc51d8ffa63fdeb0`。已读取所有提交的完整 diff 和两条合并记录的最终净差异，临时参考目录 `/tmp/bewly-upstream-20260910/`。只 fetch 目标对象供审查，没有 checkout、merge 或 cherry-pick；远端职责不变，未向任何远端写入。

| 完整 SHA | 最终功能语义 | 本地落点及状态 |
|---|---|---|
| f8e18fec88b0bd4fb62233a86d84e3b65e2f5f15 | 缺失父评论共享占位 | 已有等价保留；`utils/commentMissingParents.ts`、`inject/commentMissingParents.ts` 的父链、后到真实节点、账号/稿件清理回归保留 |
| 4b15cf9530c3d6dca5630202ff63b5070f2fb16c | 播放器滚动位置选项 | 已适配；`player.ts`、`storage.ts`、播放页设置，默认 `sendingBar`，可选 `playerCenter`；受导航/媒体源/实际模式/可见性约束 |
| 2e527075a45ee4ac586fff0d25ea550baae3b783 | 收藏封面与中性背景 | 与下一提交合并吸收；不引入过渡期收藏专用设置页或旧独立开关 |
| f2ae2e38270f72dc9e6972934841952192fd09d1 | 共享侧栏封面设置及按钮约束 | 已适配 `enableSidebarCoverBlur`、`CoverSidebarSurface.vue`、`variables.scss`；Favorites / WatchLater / History 共享表面；保留播放按钮原高度并补 flex 约束。当前 Nocturne 没有 `MomentsHotSearch.vue` 或对应挂载入口，不新增另一套热搜卡；壁纸变体按既定策略不引入 |
| 240c77b2b8311f16985fe9b0d2dcb63a6b54084b | 新版合集自定义播放控件 | 已适配；`customPlayControls.ts` 负责真实目录宿主、DOM 和样式，`randomPlay.ts` 保留顺序/媒体/路由拥有权；重挂载原控件，单个待执行更新不被持续 mutation 推迟 |
| 58667463f874ac4dac0e7ce97b51302d0eaf0a85 | 他人公开收藏夹内容读取 | 已适配并补齐来源契约；type 11 / 21 贯穿目录、资源、分页、预载、播放队列、原生入口和取消收藏。`favoriteResource.ts` 统一复合 key 与可播放/音频/失效判定 |
| 3de2674a3768b3ec7df1507e9ef49d99b4dd1377 | 拖选与悬停预览互斥 | 已适配 Home 空白区域、VideoCard 与动态/转发卡；按键拖动不启动预览并取消等待；共享 swipe seek / 原生控制条 / 全屏及原延迟保留 |
| fea31ecfb0ab12c325a18166ed5f50bf9ff0d8cb | 竖屏放大控件避让 | 已适配；`verticalVideoZoomGeometry.ts` 统一宿主 CSS 坐标、画面/黑边、左右侧栏和顶部控件避让，窄空间缩小或隐藏缩略图；原位锚定不变，普通 pointermove 不读几何 |
| 5c20bef8049e2116fbbc4f82d04ee27fbbfacea5 | 动态评论最新/最热 | 已适配本地 `MomentCommentSection`、线程与 session cache；sort 0 / 1 进入身份，每条动态只保留最近排序；旧读取失效，已发点赞由同一账号/目标缓存处理结果 |
| e82e3b13303ad8dcfa07fcfe5db9d92f073ca773 | 滚动设置校验与搜索 | 已适配持久化归一化、导入枚举、实际设置表单、选项搜索词和四语言 |
| de2e5318a62d0f63f2cb94f41044fa676c0370f8 | 合并播放器滚动设置 | 已核对净差异，功能由上述两项承担，不重复实现 |
| 73dcc1a23c15a0b74f9c1b18c30cf5e8a56c27c9 | 转发编辑器/抽奖详情翻译 | 本地按需等价保留；本地使用 `forward_*` 四语言和自己的 Composer，不调用 `repost_load_failed`、`lottery_details`、`repost` 三个上游键，不导入无调用文案或抽奖功能 |
| e89899422e6920dad9a67fafdc51d8ffa63fdeb0 | 合并 main 与动态翻译 | 已核对净差异，不重复执行合并功能 |

`dd0466bb9b22c1ea66700207765584bc4daadb3c` 的转发身份与预览是现有基线，继续由 adapter、`useMomentPreviews` 和共享媒体会话负责；本轮未重做。PGC 原位播放器、MAIN React 桥、动态转发在途事务、统一关系真值、CardRowMetrics / momentColumnIndex、首页缓存与路由、私信及三档页面模式均保留。

### 正确性与重构边界

- **稍后再看**：`updateOwnedWatchLater` 在 await 前固定账号、CSRF、媒体与操作 owner；等待 aid、成员权威读取及发送前复核。单视频入口覆盖 VideoCard、动态、原生工具栏、WatchLater 页及 TopBar。添加/移除只解释一次；已发请求不自动重发，同账号结果继续对账，旧视图不能写入新状态。卡片按媒体标识而非对象引用保持操作锁，同媒体元数据替换不重启操作；原地改写媒体字段会取消未发写入并失效旧 aid 查询。原生按钮按账号更换生命周期，旧请求最终处理不会禁用新账号按钮。批量清空与打开后移除保留各自语义。
- **收藏**：`useFavoritesData` 分开自有目录、订阅目录和内容分页状态，无 50 项目录上限；完整、失败、无进展分开，矛盾页可手动重试。重试使用该页开始时的计数，修正信号后不会把先前显示的半页误判为永久停滞。`favoriteSeason` 的 API 全部由调用方注入；公开夹保留原始资源字段，UGC 只补该来源缺省值。公开夹最新按 `fav_time`，播放保留 medialist 队列；UGC 保留已有末集语义，未确认完整/顺序时走原生入口。
- **收藏性能**：`favoriteAvatar.ts` 只补缺失头像，同 MID 合并，在途峰值 4、LRU 256、成功 10 分钟/失败 1 分钟；正文不等待头像。列表只提交新增/变化项，未变对象保留；页面只持有一份可写资源列表，删除原 `loadedSeasonMedias` 副本和 TopBar 重复 DTO / 转换逻辑。
- **播放器**：模式任务有 applied / cancelled / failed 终态，只有匹配任务释放 started；失败记录实际 readiness，只在签名变化或显式生命周期重新评估。`playerMedia` 区分媒体、外层锚点与内层模式容器；`customPlayControls` 拆出宿主/控件职责，顺序算法仍由原模块持有。HUD 用单一可替换隐藏任务和共享 token，teardown 释放。
- **评论**：仍使用本地评论/线程/树线组件。LRU 预算为 16 个会话，只存每条最近排序；活动视图及在途写入暂不淘汰，释放视图/完成写入后立即回收，避免缓存驱逐使已确认点赞倒退。序列化快照不可变，保存一次 stringify、恢复一次 parse，避免两次 JSON 往返；单快照 4 MiB、合计 8 MiB 的 UTF-16 文本预算，超预算不保存完整快照，不截断当前列表。点赞覆盖状态归原账号/目标，排序与重挂载保留 pending 锁；新视图显示已确认快照，原写入成功后更新，失败回滚，不自动重发。读取携带该目标的写入版本：旧读取不能覆盖刚完成的点赞，后发起的权威读取移除本地覆盖值；回复仅对本次实际返回的对象对账。
- **侧栏**：共享组件承担唯一装饰封面层和中性色板；缺少/加载失败的封面回到可读中性表面。删除两页重复封面 DOM、裸白色和内部 backdrop-filter；原有封面显示断点、按钮高度、胶囊/平滑圆角保留。`sidebarCoverSettings.ts` 在原始存储/导入数据上迁移旧键，新键存在时不让旧值覆盖；旧键删除通过现有序列化 patch 持久化。
- **关注目录**：批量更新时间与删除分组不再逐成员 findIndex / 全表 map；一次计算统一提交，未变对象保留。并发读的成员修正使用一次性派生 Map，原成员版本、删除标记和账号保护保留。
- **私信**：`useConversationViewport` 只拥有滚动意图、可见消息锚点及 RAF；`useConversationPresentation` 只拥有展开、离场、逐条揭示与尺寸监听；`ConversationView` 保留唯一会话身份及现有 reader/writer。移除重复 layout generation，导航/会话由原 activation generation 负责，阅读意图用局部 generation。外框、输入栏、草稿和材质实例连续性不变；模板/CSS 没有因拆分改写。

### 协议依据

本轮从实站空间页的 `script[src]` 读取原生客户端 `https://s1.hdslb.com/bfs/static/shanks/fresh-space/assets/index-5a0f1eb8.js`，对应去身份化契约在 `tests/fixtures/favorites/native-space-contract.json`。确认 FAVLIST=11、COLLECT=21，公开夹 `/x/v3/fav/resource/list`、默认 mtime/fav_time；UGC `/x/space/fav/season/list` 返回后由客户端本地切片的全量兼容；rE / bB 分别取消公开夹 / UGC 订阅，POST query 的 ID 和 CSRF 不可混用。没有使用失效文档或镜像作为当前证据；没有对真实账号执行取消收藏或其他写操作。此证据是版本化客户端源码，不是成功写入抓包。

### 本轮实际验证

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm knip` 最新完整运行退出码均为 **0**。项目测试依次执行 targeted-fixes、selected-p2、private-message、notifications，日志有 **318 条 PASS**，另含定向组合脚本通过信息；没有删除行为断言或降低门禁。迁移后的源码位置断言改指向实际模块，并保留/补充组件行为测试。补充覆盖同媒体对象替换、原地 BV/CID 修改、换号和卸载后的真实卡片预览请求；与稍后再看共用卡片账号/媒体生命周期，没有增加第二套请求控制器。
- 新增 `verify-watch-later-ownership.mjs`、`verify-favorite-sources.mjs`、`verify-upstream-settings.mjs`，扩展现有播放器、动态、关注和私信测试。覆盖未发请求换号/Cookie 变化/卸载零写入、已发响应与重复点击，type 11/21、51/120 项目录、独立失败、重叠/停滞/矛盾页及修复后重试、队列/原生修饰键/停止 runtime，模式耗尽/重试/旧任务、HUD 双提示，评论 0→1→0 乱序/换号/重挂载/点赞中切换，拖选/seek/全屏，私信 A→B→C、输入节点与草稿、锚点/隐藏恢复/已读，配置迁移/导入与实际设置表单。
- Knip 仍列出 **51 个既有未使用导出、5 个导出类型、4 个枚举成员**；没有新增忽略或关闭检查。原先 TopBar 重复资源 DTO 已删除；其余不属于本轮替代路径的提示未扩大清理范围。退出 0 不代表无提示。
- 已重启本项目 `pnpm dev`；MAIN、内容脚本、后台、最小首屏脚本均编译成功，后续增量也成功。新构建标记 **mtuvyu7b**；没有执行 `pnpm build` 或其他独立 build 命令，没有修改其他 worktree 的开发进程。运行中的开发会话继续保留。
- `git diff --check` 退出 0；HEAD 仍为 `c03f9c4d`，与 origin/message_feature 提交差异 0/0。没有暂存、提交、推送或改写历史；原 stash 哈希未变。

同一合成输入的性能计数（不是实站 FPS 或堆内存结论）：

| 实际模块 / 输入 | HEAD 基线 | 当前工作区 |
|---|---|---|
| 关注目录 1,200 人，更新其中 600 人的时间 | MID 读取 189,141 次；目录提交 2 次 | MID 读取 7,641 次；目录提交 1 次 |
| 同一目录移除 600 人所在分组 | MID 读取 541,500 次；目录提交 600 次 | MID 读取 1,200 次；目录提交 1 次 |
| 2,000 条评论，20 次保存/恢复，约 2,170,218 字节 UTF-16 快照 | stringify / parse 各 40 次；一次本机样本 201ms | 各 20 次；同次输入样本 102ms，快照仍独立 |
| 300 个缺失头像 MID + 已有头像 | 原路径在列表后等待补全 | 在途峰值 4，缓存 256；同 MID 合并，已有头像 0 请求；正文先提交 |
| 3,000 条动态、两列 | 保留原有索引与窗口实现 | 100 次等价窗口 0 赋值、100 次重复 ref 0 注册、该测试窗口挂载 42 卡 |
| 竖屏控件 / 播放器布局 | 保留已有 RAF 合并 | 100 次普通 pointermove 0 几何读取；100 次尺寸信号 1 RAF；100 次等价布局 0 原站 resize |

### 本轮实站与未验证项

- 已核实原生空间页实际资源脚本与公开夹/UGC 的类型、接口、排序字段和取消订阅契约；仅浏览和读取，没有真实账号写入。
- 用户已重新加载扩展；之后在收藏、视频、PGC、私信和动态页核对到 **mtuvyu7b**，以下实站结果均来自该新构建。先前 `mtsr4vh6` 的检查不计为本轮通过。
- 收藏：订阅目录实际从 **50 项加载至 90 项**，末页去掉加载更多；切换现有订阅来源后成功显示内容。封面开启/关闭、缺少封面、亮色/暗色/OLED 的侧栏与文字配色已核对；关闭后图片节点移除，选择器 `backdrop-filter: none`，播放全部按钮高 **36px**。
- 播放页：`BV1Zibx6yExR`、`BV1yDtV6XEB8` 均有完整侧栏和原位固定播放器，原版右栏保持 `display:block / visibility:hidden`。仅打开并关闭原生投币、收藏窗口，确认事件和弹层存在，未提交。通过可访问性按钮完成退出/重新进入 Bewly 模式，原版操作/推荐区恢复。当前合集没有 `.auto-play/.continuous-btn`；临时开启自定义播放时，`.video-pod__header` 内有且只有一套顺序选择、开关和编辑按钮，关闭后控件数恢复 **0**。
- PGC：`ep1231540` 使用 `#bilibili-player-wrap` 原位固定布局，控件和侧栏 ready；原生 **101–150** 分段可操作，选择 `ep1231541` 后 URL、标题、时长 **1213→1208 秒** 更新，最终 video readyState **4**，原位模式及侧栏继续就绪。未绕过会员或地区限制。
- 桌面窄视口：使用浏览器提供的临时 viewport 能力测试 **1100×800**，原位播放器宽 1100，侧栏宽 **935（85%）**，右缘距视口 16px，内部无横向溢出。测试后 reset 回原 **1744×1027**；未测试手机设备/分辨率。
- 私信：为避免改动真实未读，先临时关闭自动标记已读。普通会话首次展开后为 `expanded / ready`，无骨架；连续切换后的 textarea 可访问性节点保持 **560**，消息区域切换而输入栏连续，选中用户与详情一致。左右边线各 **1px**，输入外壳圆角 **24px**，与左栏底部差 **0px**；上下滚动不收起，重复点击当前会话后详情卸载并清除 URL 会话参数。表情面板读取成功，显示 **17 个包标签 / 242 个图片节点**，图片选择入口存在；未选择真实文件或发送内容。
- 动态：实际展开本地评论，默认选中“最新”，取得评论；切换“最热”后收起再展开仍恢复“最热”，没有接口错误或第二套评论组件。
- 临时修改的主题（原为设备）、OLED（原为开启）、侧栏封面（原为开启）、自定义播放（原为关闭）、自动已读（原为开启）均已恢复；只在关闭当前私信会话后恢复自动已读。浏览器视口覆盖也已解除。
- 尚未完成的实站组合：SS 入口、真实多 P 与播放全部队列跳转、结束推荐/后台返回/Drawer、左右侧栏与竖屏黑边的完整组合、浏览器真实缩放/大字号、预览拖选/全屏及真实换号过程。它们已有对应模块/组件回归，未宣称这些未执行组合全部实站通过。
- 真实稍后再看、取消收藏、关注/分组写入、评论点赞、私信发送及 ACK 未作为实站测试执行。当前不做未经授权的真实账号写入；新取消订阅协议的证据来自原生客户端源码及对应 fixture，未声称有真实成功请求。
- 未进行实站长任务、资源存活及浏览器堆内存的同条件对比；只报告上述可复现模块计数，不推算 FPS、整页内存或收益百分比。

### 2026-09-10：动态详情跨域重定向消息修复

- 根因：`www.bilibili.com/opus/:id` 重定向至 `t.bilibili.com/:id` 后，iframe 的 `src` 不变；load 回调将旧 origin 当成就绪目标，导致视口同步触发浏览器的 targetOrigin 不匹配错误。
- `iframeMessage.ts` 对动态详情的跨域 load 不再直接确认就绪；复用 `BEWLY_OPUS_LAYOUT_READY`，由子页附带实际 href，校验来源 Window、HTTPS 域名与同一动态 ID 后记录实际 origin。缓存绑定完整 src 和 contentWindow，后续视口、关闭与图片查看消息共用该目标；不使用通配 origin，不新增监听或轮询。
- 实际模块回归覆盖重定向前零发送、就绪后正确域名、load 晚于就绪、错误窗口/域名/动态 ID 拒绝及 src 改变后的旧消息失效。
- 本次 `pnpm test`、`pnpm lint`、`pnpm typecheck`、`pnpm knip` 均退出 0；Knip 保留已有提示。现有 `pnpm dev` watch 已成功完成增量编译，未执行生产 build。
- 本次补丁之后尚未重新加载扩展并完成实站重定向验收；上文 mtuvyu7b 的既有实站结果不代表本次补丁已验收。人工检查：重载开发扩展、刷新动态页、打开发生跳转的动态详情，确认视口同步、图片查看和关闭正常且控制台无 origin mismatch。
