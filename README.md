# Bewly_Nocturne

Bewly_Nocturne 是一个面向个人使用习惯定制的 Bilibili 浏览器扩展。它仍是浏览器扩展，不是 macOS、桌面或移动客户端。

## 来源与维护

Bewly_Nocturne 基于 [BewlyCat](https://github.com/keleus/BewlyCat) 开发；BewlyCat 基于 [BewlyBewly](https://github.com/BewlyBewly/BewlyBewly)。本仓库保留原项目历史和贡献者信息。

本项目会继续选择性合并 BewlyCat 上游更新。无冲突的更新正常接收；与 Bewly_Nocturne 定制行为冲突时，经人工审查后保留本地定制。

维护者的分支、Fork、cherry-pick 与上游同步流程见[上游协作流程](docs/maintenance/upstream-workflow.md)。

本项目仅接受外观和视觉交互的issue，底层功能请向上游提交。

本项目假设用户有超过平均线的运行设备，故任何性能和外观之间的取舍都会偏向于外观。

## 夜曲设计一览

Bewly_Nocturne 并不是 BewlyCat 的纯镜像。它在保留 Bilibili 内容能力的基础上，维护一套独立的夜曲视觉与交互设计：

| 区域 | Bewly_Nocturne 的设计 |
| --- | --- |
| Dock | iOS 风格完整胶囊；未选中项没有圆形底板，选中指示、Hover 发光和刷新后居中保持连贯；浅色使用主题色光晕，深色使用白色发光选中效果。支持按钮收起、隐藏收起入口、离开后自动收起三档；同一玻璃外壳从原 Dock 中心连续形变为圆球，展开时外壳完成生长后再加载按钮组。 |
| 页面与顶栏模式 | 支持“全 Bewly / 全原版 / 自定义”；顶栏类型在 Dock 内容设置中配置，不作为导航项。全原版会尊重 Bilibili Evolved 顶栏。 |
| 毛玻璃 | 默认开启，模糊强度全局统一。一个“关闭毛玻璃与顶栏渐变”选项即可切换到完整实色版本，不维护彼此冲突的多个开关。 |
| 深色网页背景 | 使用 OKLCH 限制最终明度和色度，让不同深色基准色仍保持稳定、舒适的页面底色，同时保留轻微色相倾向。 |
| OLED 纯黑 | 仅在实际深色主题下把网页底层变为 `#000`，卡片、面板和弹窗继续保留层级，不以覆盖原站布局的方式实现。 |
| 平滑圆角 | 卡片、面板、输入控件和媒体使用统一的连续曲率；圆形按钮、头像、Switch 和完整胶囊仍保持真实 round。浏览器不支持 `corner-shape` 时自动使用原有 `border-radius`。 |
| 表面与边框 | 深色玻璃表面使用克制的透明层级和不透明边框；设置页、Dialog、Popover 与媒体外框遵循同一套语义 token。 |
| 设置体验 | 设置窗口从实际 Dock 按钮位置展开；顶部渐变模糊自然衰减且不影响清晰外框；新版本与重载提示共用同一 round 轮廓，登录入口和共享关闭按钮使用实色表面保持清晰。 |
| 设置归类 | 毛玻璃与普通顶栏渐变由一个负向开关统一控制；主题色渐变只在外观中配置；内存节省选项集中在“通用 → 内存节省”；“Bewly 页面”二级导航预留独立的播放页和消息页设置框架。 |
| 主题色可读性 | 实色主题背景上的文字与图标会根据最终主题色亮度自动选择黑色或白色，近白主题色也能保持清晰。 |
| 精简功能 | 背景图片功能已完整移除；项目不保留壁纸入口、存储字段、渲染层或旧数据兼容代码。 |
| 运行稳定性 | 扩展重新加载和消息通道失效使用统一处理；开发内容脚本保留可读函数名，避免长期增量编译产生调用错配。 |

这些设计属于 Bewly_Nocturne 的维护边界。选择性合并 BewlyCat 上游更新时，会逐项审查 Dock、顶栏、页面模式、主题、圆角、毛玻璃和设置交互；上游实现不会直接覆盖这些本地设计。

更具体的实现约束和冲突审查基线见 [AGENTS.md](AGENTS.md)。

## Stage 2 变更

### 添加了

- Dock 的按钮收起、隐藏收起入口和离开后自动收起三档模式；同一毛玻璃外壳围绕原 Dock 中心在完整胶囊与圆球之间连续伸缩。
- “Bewly 页面”下独立的播放页与消息页设置框架，为后续页面设置提供模块边界，当前不增加设置字段。
- 按 Bilibili 账号隔离的首页推荐、动态和关注流缓存，切换账号或退出后不再显示上一账号的内容。
- 设置云同步状态提示，可查看未同步字段数、容量受限数量和最近一次同步错误。

### 修改了

- 修复了动态、历史、稍后再看、番剧订阅、关注流和首页榜单的尾页丢失、登录状态不复位、删除错项与旧请求覆盖新内容等问题。
- 稍后再看统一以 `aid` 识别视频，顶栏、视频卡片、动态和原站页面的成员状态可跨标签页同步。
- 修复了搜索建议竞态、非法正则导致首页中断、搜索参数异常和浏览器前进/后退重复写入历史的问题。
- 分享链接净化会保留 `p`、`t` 和 hash 等导航与播放参数，不再破坏多 P 视频定位。
- 修复了 Firefox Multi-Account Containers 中 Cookie 串用、iframe/PIP 关闭资源残留、页面通信串请求和设置未就绪时请求一直等待的问题。
- 站内跳转现在共用同一路由状态，页面模式、深色模式和顶栏能在 SPA 导航后及时更新；截图、画面比例和触屏手势仅在对应功能开启时运行。
- 液态分段指示器固定保持现有视觉；版本与重载提示、登录入口、关闭按钮及设置页框架继续复用夜曲语义表面。
- 扩展重载后，旧页面的失效消息会终止轮询和异步错误传播；Shadow DOM 样式加载失败时不再展示透明、错位的无样式 Dock 或设置页。
- 修复了未登录或顶栏元素尚未挂载时引用被错误解包、导致 Bilibili 页面报错的问题。

### 去除了

- Bewly 自己的全局播放器与页面快捷键及其设置入口；Bilibili 原站快捷键和 Dialog、Drawer、图片查看器等局部键盘操作保持不变。
- 液态分段指示器的可见设置项、搜索入口和废弃说明；运行时效果本身保留并固定启用。
- 已无 HTML 入口或扩展运行流程依赖的 `build:web` / `dev:web`；开发命令只启动仍有效的扩展编译任务。

## 本地构建

```sh
pnpm install
pnpm dev
```

开发模式不会压缩内容脚本，以保证长时间监听编译时的函数引用稳定；生产构建仍沿用正常压缩流程。重新编译或重载未打包扩展后，已有 Bilibili 页面需要刷新一次以接入新的扩展上下文。

Chrome 生产版及 ZIP：

```sh
pnpm build
pnpm pack:zip
```

构建目录为 `extension/`，压缩包为 `extension.zip`。

## 许可

本项目使用[基于 MIT 并附加使用限制的自定义许可](LICENSE)。额外限制包括禁止将项目封装、转换或发布为独立客户端。复制、修改或分发本项目时，必须保留适用的版权声明与许可文本。
