# 2026-09-16 上游体验与生命周期适配

本轮唯一维护记录。实施范围：`e89899422e6920dad9a67fafdc51d8ffa63fdeb0..4c1e1241e025eee39ec0cf4f6b6c5132ad21a6a7`；按最终行为移植，没有 cherry-pick、覆盖上游整文件、改版本、提交或推送。

本地从 `message_feature` 的 `c044f261b6ab5c3e68082d3dfd2b9750f2d538f0` 继续。请求中的审查基线 `d2b32f3429274c5a25d61491a9e7d7d0a8274fee` 不在当前本地对象库中；未回退工作区。已核对 origin/contrib/upstream、跟踪关系和工作区。原 stash `7a6ef9155ccd8e5ce92768702da03b5ae8d7184b` 保持不变。

## 上游对应表

“已适配”指源码与模块/组件回归完成，不表示未经执行的实站操作验收通过。下表仅列本轮批准的功能，以及直接相关的本地等价与产品策略差异。

| 项 | 完整上游 SHA | 最终语义与本地落点 | 处理 |
| --- | --- | --- | --- |
| 01 | `8795b12a4ae4f86543334f06dbe2157f0b874e46` | `VideoCardGrid` 用已有窗口加载范围补关系；`useUserRelations` 合并 MID 和在途读取 | 已适配 |
| 01 | `4e60cc9176f5a7e4614fb3b431b7005e8b212aea` | 成功的 null/空关系为权威空结果；失败/畸形 map 不覆盖；仅 `getRelations` 的 fids 保留字面逗号 | 已适配 |
| 01/10 | `eb4b7e5e740d145ee714083877d30d0319d3d2ee` | 关系按消费者释放；本地评论已有页面/账号 owner、16 会话预算和在途点赞保护，保留 | 关系已适配；评论等价保留 |
| 02 | `52ce81f611df14c013ab413a49810840c7bce15c` | `useSettingsStorage` 原位对账、删除与引用保持；`storage` 归一化进入同一路径；MAIN 只监听序列化的协议字段 | 已适配 |
| 03 | `1ded30080ef802874fbc58b38eded92d4fd93355` | 原生信息流按页面模式/iframe 所有权启停；隐藏清空候选引用、去重、根替换发现 | 广告部分已适配；壁纸按产品策略不引入 |
| 04 | `49f3cdb135f03fea9ef12d64305cbe758de6ff2a` | `useHomeTabState` 消费 ref/reactive 快照，mounted 消费结束释放其余引用；ForYou 不保留已禁用的缓存 | 已适配；保留标签数据缓存功能 |
| 04 | `d59a29cc3780d08e90dfcfed20b26f82ba4435b0` | 首页入口置顶与标签内位置恢复继续由 `Home.vue` 的既有意图处理 | 本地等价保留并回归 |
| 05 | `ceb6df7b8fbc2de354c9669c1494ed31a9ce4968` | `scrollIntent` 统一显式滚动代次；`useCardWindow` 不用旧锚点打断回顶，RO 只测脏行、书签同轮合并 | 已适配 |
| 06 | `5816a93df587c5ce118df875860e5f52f8be22e5` | `authProvider` fetch 与 JSON 读取共用真实 15 秒期限；QR 按视图 AbortController；刷新 token 保留独立 single-flight | 已适配；原有串行轮询协调器保留 |
| 07 | `b79586cc520723fb5c3385bd00394f42ef365902` | `contentScriptRefreshPrompt` 区分启动、不可达、版本和安装身份，有限 ping/重试 | 已适配 |
| 07 | `ef2ee92e0615ae89ec1fd08ff884881bf07ab105` | 诊断绑定 tab、URL 与文档时间；同 URL 重载/关闭失效；更新提示保留按钮实例 | 诊断已适配；后续四项功能任务补齐批量刷新，见下节 |
| 07 | `72e7ab043a0c2a195225e43b14ba44c2e1dd981c` | Git SHA 仅诊断，不作为兼容判定；健康恢复撤销提示 | 已适配 |
| 08 | `203d589d6fce80b61511ff666c48e9ad0bca990b` | `useFollowingVideoSearch` 读取真实 WBI 投稿接口，草稿/提交词分离，MID/账号/分页归属；联合投稿保留查询 UP 与作者区别 | 已适配；壁纸不引入 |
| 09 | `90ac769d292b2cf9f37ca78b54ff6c26345c9732` | `FollowingSidebar` 复用 `followingUploaderSort` 的不分组/按分组快捷切换 | 已适配 |
| 09 | `5e851c8519bd6c7a12febfc6be2d56cf82f12d6e` | 控件固定、目录统一滚动，无每组独立限高；大目录复用 `useCardWindow`，保留焦点与分组菜单 | 已适配 |
| 10 | `4269610168c60f4255436b5956d8ac8a333ae03f` | `commentThreadLayout` 复用缺失父链纯函数；同父共享占位、后到真实父替换；占位参与折叠/树线 | 插件评论已适配；MAIN 既有实现保留 |
| 10 | `776968f872debe142e9b4f5443c0a6dfb947a028` | 评论根分页自动填充、停滞可重试；本地转发编辑器增加 @/# 补全、文本表情、IME/ESC/焦点与迟到结果清理 | 已适配 |
| 11 | `d66d5a2ecdaa9dddd33c841bbf3be0f4b930de38` | `linkNavigation` 的手势判定区分拖选、清除选区、长按和滚动后 click | 已适配 |
| 11 | `b8ab9bee26b64b1c7206de22ab09d7684b48028b` | 视频封面、转发视频和正文视频链接统一交给 `useMomentDetail.openVideoLink`，遵守已有打开设置 | 已适配 |
| 11 | `d8522b7960e9b7a137f09a20ab921d1245882e90` | 动态主入口保留真实链接；中键/修饰键原生行为、键盘普通激活保持 | 已适配 |
| 12 | `4a2b48a5ab2272a2b8937b4bb9a3edc335eb8b05` | `videoScreenshot` 共用动作/忙状态，采样前固定画面与名称；按钮/独立可配置快捷键解耦，四语言和导入校验 | 已适配 |
| 13 | `885f1677d732f6e929ebeba01e06d0f0c5a490bb` | 结果页 TopBar 搜索原地更新；异步写历史后复核路由代次；保留 history.state 和其他入口策略 | 已适配 |
| 13 | `5a4c9329d96d18914fe5c84d69d388ab9ff41c11` | 默认词后台专用 10 分钟/16 分区缓存、请求合并、session 恢复；无需求/隐藏不拉取；热搜不按账号分区 | 已适配 |
| 14 | `2b6b17f6ea38fb09d1deea6566d0f0f03df0255a` | `watchLaterList` 统一 BV/AV/PGC 的展示和合法链接，页面/专用网格/TopBar 共用；可选字段缺失不拒绝整页 | 已适配 |
| 15 | `e700dc30babe45aa9824421676b29a3f7f66b233` | `wholeMeta` 用共享 RO+RAF 批量读写，整项隐藏溢出，等价更新不重测 | 已适配 |
| 15 | `55b2f953e731996a1783de1d7596c875c89c1fa8` | `VideoCardInfo` 四个布局分支均将发布时间置于 DOM 首位；字体/候选/宽度变更可失效 | 已适配 |
| 16 | `60a752f4ca48c3e4a94194cefce435ff4bc1a92c` | `searchTransforms` 归一化直播状态/房间；`UserAvatarLink` 复用头像链接，可见时才有波纹，支持 reduced-motion | 已适配 |
| 保护 | `8470980580e87ed2da507c796c397655ce012ef9` | 本地没有上游旧宽屏控制器的页头无限等待链；继续使用现有导航/播放器任务与 PGC 原位锚定 | 本地等价保留，不重建旧控制器 |
| 保护 | `903828e7b5cbafb0a8bd087b23f80ba71cfe8902` | 本地拖宽线属于 aside，内容面板独立滚动；保留 50% 默认/85% 最大、手动宽度及键盘调宽 | 本地架构已覆盖，不复制 ROOT 搬移假设 |
| 保护 | `64d460c32a69cae54eae23d7e1bc8b725140bc4d` | 改动针对本地不存在的 `contentScripts/bewlyWidescreenControl.ts` 独立切换按钮 | 不成立于当前入口，不恢复已删按钮 |
| 保护 | `830d8208dd82424c8dc42013fa426fec3144b4c4` | `bilibiliTopBar` 只准备自定义首页的原生顶栏；释放旧 header 的观察器/登录绑定；祖先替换监听不扫描全页后代 | 按本地架构已适配 |

上一任务按要求暂不实现四个可选功能；用户随后明确批准的实施记录集中追加在本文件末尾。保留元素设置右键菜单，未移植上游删除菜单的 `bf7d8f862ab5476ff69421c26ff24838901f2b9e`。没有引入壁纸、上游全局快捷键框架或新的原版顶栏切换按钮。截图是明确批准的单独播放页动作；与历史上已移除的全局快捷键产品入口区分。

## 职责与清理

- 关系仍由原有 `useUserRelations` 写入。只增加活跃消费者、每 MID 请求/写版本与有界缓存，不新增 Store；菜单和 Following 继续使用既有关系变更事件。
- 设置仍由后台 patch 协调器权威对账；canonical → in-flight → queued 顺序、epoch/revision、flush 与恢复保留。删除重复的 ACK render 与设置整对象回写；归一化复用同一函数。
- 首页快照保留既有所有权转移。恢复值从暂存快照移出，剩余字段在恢复钩子后释放；无效 generation 在调用快照 getter 前退出。未删除历史/分页/筛选缓存来换取性能。
- 关注投稿搜索从页面剥离为独立查询职责，普通动态列表/目录写事务仍各有原 owner；不传递整页 refs。分组菜单、事务及成员版本保持原实现。
- 动态评论只新建父链到展示节点的适配，继续使用本地 session/thread/tree guides。折叠状态进入同一快照；评论排序、在途点赞和转发事务保持。
- 截图编码、忙状态与 URL 清理移出按钮挂载器；按钮、快捷键共用。移除原重复编码/下载逻辑和 auth 的外层重复 single-flight 包装。
- 删除仓库内已无引用的旧 `BaseShortcutSetting` / `ShortcutsSettings` 类型；不复活已移除的全局快捷键系统。窗口启用阈值由 `useCardWindow` 统一命名，三处消费者共用。
- 搜索默认词缓存只属于后台该 endpoint，不扩展成通用请求框架。热搜仍属于现有共享搜索 owner。
- 稍后再看保留专用卡片与批量业务入口，扩展现有 `useCardWindow`；删除导致列表离场定位叠加的 TransitionGroup，焦点、单项在途操作及布局编辑阻止回收。未新增第二套高度计算系统。
- 实际组件测试发现并修复稍后再看 `pendingAction` 被深层 Ref 代理后与原对象比较失败的问题；改用 `shallowRef` 保存操作身份，完成后按钮与离屏回收均正确释放。分页结束依据原始返回页长度，缺必需身份的单条数据不会把整页误判成尾页。
- 元数据溢出与用户头像分别收敛为一个共享 directive 和一个链接组件；不为每个标签增加 observer。
- 实站回归补齐两处交接：手动话题面板使用正确的 autofocus 默认值，Esc 关闭话题/表情面板恢复输入焦点；运行时就绪标记移到 `onDOMLoaded` 成功完成处，避免只在 SPA 导航后才解除“初始化中”。取消或挂载失败不能标记健康。
- 本轮没有修改私信会话外框/输入实例连续性、玻璃材质参数、顶栏渐进模糊、OLED 与 PGC 原生 React 事件桥。

## 协议依据

- 投稿搜索：上游 [`203d589d6fce80b61511ff666c48e9ad0bca990b`](https://github.com/keleus/BewlyCat/commit/203d589d6fce80b61511ff666c48e9ad0bca990b) 的 `/x/space/wbi/arc/search` 与 keyword/pn/ps 契约；本地沿用后台 WBI。
- @ 候选与提交：[目标版本 moment API](https://github.com/keleus/BewlyCat/blob/4c1e1241e025eee39ec0cf4f6b6c5132ad21a6a7/src/background/messageListeners/api/moment.ts)、[转发组件](https://github.com/keleus/BewlyCat/blob/4c1e1241e025eee39ec0cf4f6b6c5132ad21a6a7/src/components/MomentCard/MomentRepost.vue)。`/x/polymer/web-dynamic/v1/mention/search` 返回 groups/items，提交 type=2 与 MID；文本表情不转换为图片 type=9。
- 默认词缓存分区：[5a4c932 完整 diff](https://github.com/keleus/BewlyCat/commit/5a4c9329d96d18914fe5c84d69d388ab9ff41c11) 对该带凭证 endpoint 使用 DedeUserID/私密窗口分区。此处沿用这一已审实现，没有推断其他公共接口也需要账号缓存。
- PGC 展示字段：[2b6b17f 完整 diff](https://github.com/keleus/BewlyCat/commit/2b6b17f6ea38fb09d1deea6566d0f0f03df0255a)。缺少可播放剧集身份或不支持的重定向时不构造假视频链接。
- 新增测试使用虚构 MID、视频、评论与可控 Promise；没有采集或保存用户 Cookie、消息正文与账号写入结果。

## 验证与测量

主要新增行为回归集中于 `scripts/verify-september-adaptation.mjs`，通过 `pnpm test` 的 `verify:selected-p2` 注册。评论自动填充/重复页/精确页码重试加入真实 Vue 生命周期测试 `verify-moment-comment-lifecycle.ts`。原行为断言保留；已有 fixture 仅补真实模块依赖或迁移后的入口。

已执行的测量均为相同输入下的模块/组件夹具，不是实站 FPS 或内存收益：

- 1200 人关注目录，首屏实际挂载 41 人；滚至远处时原焦点 DOM 实例保留。
- 500 条稍后再看数据，首屏实际挂载 15 张专用列表卡片；在途操作/焦点保留同一 DOM，结束后可回收；切换网格后第 500 条仍可达。
- 600 个活跃关系项不因预算被清除；释放后保留 512 项。重复 MID/在途读取合并；成功 null、失败/畸形数据、Cookie 先变化与旧读取覆盖新写入均有断言。
- 100 次等价标签更新没有额外测量；宽度与字体/候选变更才重新安排测量，标签内部裁切也按整项隐藏。
- 广告隐藏后候选 Set、待执行 RAF、观察目标均为 0，不保留脱落的旧信息流根；恢复后发现新根并重新扫描。
- 两个同账号默认词消费者共用一次网络请求；换号使用另一分区，后台实例重建仍可恢复未过期缓存。
- 保留原播放器夹具：100 次相同布局请求没有新增 resize 广播；这不替代真实播放器验收。

只使用 `pnpm dev` 开发编译，没有执行生产 build/打包。开发构建标记：`mu3k292a`。该标记在同一 dev 进程的增量编译间保持不变；最终增量完成后，已通过原生 Chrome 管理页重载 `Bewly Nocturne Dev` 并刷新验收文档。

## 实站验收状态

初次旧页面标记为 `mtvb36xx`，其表现未计入本轮通过项。连接恢复后，已在新建的独立 Chrome 标签中确认 `mu3k292a` 并完成下列实站检查：

- 首页初次进入时 `scrollTop=0`，Logo 与搜索区可见；元数据时间位于首项。
- 新关注布局：真实 UP 投稿搜索返回多年前的视频；输入新草稿但不提交、切换分组后，原结果、选中 UP 与新草稿均保留。目录只有一个滚动区。原先关闭的新布局开关与按更新时间选项均已恢复。
- 1100×800 桌面窗口未见此次控件重叠；抽查 24 个元数据行，可见标签的越界/内部裁切计数为 0。浅色、普通深色及 OLED 均查看过页面表面，最终恢复“跟随设备 + OLED”。未测试移动端。
- 原生 Chrome 菜单执行一次放大：CSS 视口由 1744×1027 / DPR 2 变为 1585×933 / DPR 2.2，动态布局仍正常；随后缩小一次，核对原尺寸/DPR 完整恢复。
- 动态评论加载与最热排序正常；稳定布局下收起读数为 `866 → 866`。首轮工具点击记录过 43.5px 差异，未分离工具自动定位与迟到布局因素，未据此宣称所有加载阶段都零位移。
- @ 用户目录真实读取成功；选取后插入草稿并恢复编辑焦点，测试文本已清空，没有发送。最终增量重载后，手动话题面板打开聚焦输入框，Esc 关闭回到转发输入框，再按 Esc 关闭空草稿。
- 搜索结果页顶栏在原标签更新 URL、标题与新结果；综合与用户分类中的直播头像指向实际直播间，个人空间链接及关注按钮独立，未见嵌套按钮。
- BV `BV14ReF6NEWN`：独立 Bewly 布局、原生控制栏、作者、操作栏、简介、标签与评论可见；原生分享浮层可打开。退出后 Bewly shell 移除，原播放器仍连接，原站控件恢复。
- PGC `ss105212`：侧栏季信息、评分、简介、追番状态、季切换与排序入口可见；通过原生选集切换到 `ep5137672` 后标题正确更新，Bewly shell 保持，页面只有一个 video 节点。
- 稍后再看真实列表读取正常，滚动进入窗口化后观察到上方 2658px 占位、120 个挂载网格槽位，页面未出现错误提示。未执行播放并移除、删除、清空等写操作。
- 激活页面时发现的“已可用却仍显示初始化中”误报，已通过实际启动入口测试和再次重载后的首次加载/激活检查修复。该项不以 SPA 导航掩盖首次启动问题。

尚未实站验证：换号/网络悬挂/旧版本等负向时序、真实关注/分组/点赞/转发/稍后再看写入、截图 PNG 下载与二维码扫码授权、所有 PGC/多 P/合集/全屏/Drawer 的长期组合、隐藏页资源暂停（当前浏览器安装了可改写可见性状态的扩展）、所有字体/缩放档位，以及加载中收起评论的所有布局时序。对应可控 Promise、DOM/组件和既有协议回归已执行，不能替代上述真实场景。所有临时偏好和视口覆写已恢复，未修改用户的其他扩展配置。

浏览器清理：主验收标签已关闭并恢复用户原播放标签。关闭旧验收标签时 Chrome 要求永久删除已保存的临时分组，已取消该永久删除；未擅自删除用户设备上的保存分组。

## 上一任务检查

- `pnpm test`：退出 0；targeted、组件/模块、私信、通知四组执行完成，340 条 PASS 记录。包含评论排序乱序/自动填充与停滞重试、关系与 Cookie 归属、设置 ACK、授权取消/读取超时、首次启动及同 URL 刷新归属、截图导航、卡片手势/窗口/焦点/保留，以及原有 PGC/原生 React 事件桥和私信实例连续性。
- `pnpm typecheck`：退出 0。
- `pnpm knip`：退出 0，但仍报告 50 个未使用导出、4 个未使用导出类型、4 个枚举成员提示；未将其忽略或描述成无警告。
- `pnpm lint`：退出 0。
- `pnpm dev`：后台、最小首屏入口、MAIN 注入与内容脚本开发编译成功，后续增量继续由同一进程完成。没有生产 build、打包、commit 或 push。

## 后续：四项已选功能

继续原 `message_feature`，保留前述全部未提交实现及 stash。已读取下列完整 diff、最终集成及现有调用链，没有再次移植诊断、媒体释放、账号或设置框架。

| 完整上游 SHA | 最终语义 | 本地处理 |
| --- | --- | --- |
| `cb888cdaa8107fc32864f9d46b1cbce49b38d486` | 打开标签加入稍后再看 | 已适配；后台保留标签 ID、窗口、URL 与隐私上下文，取代上游纯 URL 数组 |
| `4af5471033c4d7cfcdf4a2dfdd9e5771a12660ce` | 确认、执行进度 | 已适配；复用 Dialog/Progress，增加停止、重新打开进度、失败项重试 |
| `4b580702faa896fb9f19d58bd84c8d46312de3fd` | 上述功能最终合入 | 核对净差异；未重复移植合并记录 |
| `cbe9c534166aca1194a71ded7373d0cabc03521b` | 播完自动移除 | 已适配；默认关闭，严格限定稍后再看主播放器及完整最后一 P；未搬入上游宽屏控制器 |
| `ef2ee92e0615ae89ec1fd08ff884881bf07ab105` | 刷新所有支持页面 | 在上一任务诊断上补齐显式确认、任务结果、失败重试与发起页最后刷新 |
| `b3d4d3f47ce3a4bfcd20fa380fb938a6ac9b58be` | 刷新按钮第一次点击失效 | 保留原位更新，并回归新增按钮在提示更新、焦点及超时下的实例连续性 |
| `72e7ab043a0c2a195225e43b14ba44c2e1dd981c` | Git SHA 仅作诊断 | 本地等价保留；没有恢复提交不同即不兼容的判定 |
| `244f9b7592c4c8893c203539b214a77e95838f39` | 仅首页当前标签链接模式 | 已适配到顶栏与搜索设置；视频卡模式不扩展，原默认值不变 |
| `d6eaf3484c80975ed70b0fa67c5a0fba67b6dd8a` | 去掉批量刷新失败反馈 | 按本次要求不引入；保留紧凑可见状态与失败重试 |

### 入口、职责与边界

- 批量添加入口：稍后再看页面及顶栏稍后再看浮层的“添加播放标签页”。`OpenTabsDialog` 只展示状态，`useOpenTabsWatchLater` 只持有可释放的投影；`openTabsWatchLater` 在后台持有当前隐私上下文唯一任务，`openTabsWatchLaterTask` 执行已确认快照。重新打开尚未确认的候选会重新枚举；已启动任务恢复原进度。不会自动点击确认或关闭/跳转原标签。
- `playbackTab` 先验证域名/播放路径，再复用既有视频身份解析；EP 使用已有精确分集查找，SS 无法确定当前分集时明确跳过。按 aid 去重分 P、BV/AV、重复标签及原有成员；解析结果只在本次任务内复用。成员列表只在批次开始权威读取一次，不把临时 Set 当作新成员真值。
- `watchLaterWrite` 是普通单项、自动移除和批量添加共用的发送边界；原 `updateOwnedWatchLater` 保留提交前账号/Cookie/视图检查和发送后对账。自动与手动移除同账号、凭据、aid 的在途请求合并。批次结束通过原 TopBar broker 统一失效；稍后再看页面读取同一失效版本重新取列表。
- 批量任务每项发送前保存状态。停止/换号只停止未发送项，发送后的成功仍记入原任务。service worker 重启时，发送中项标为未知，未处理项停止，不自动重发。只有明确失败项可手动重试；完整扩展重载/浏览器关闭会清空 session 记录，也不会自动恢复发送。账号 Cookie 变化复用 `loginStateWatcher`，未新增账号 Store。
- 自动移除入口：设置 → Bilibili → 播放行为。“播完自动移出稍后再看”默认关闭。`watchLaterCompletion` 保存主媒体、来源、账号 owner 和权威分 P 信息；`watchLaterAutoRemove` 使用原 `observePlayerDom`、路由和 App 生命周期，禁用/离开时移除事件和订阅。结束位置、最后 CID/分 P、完整时长必须一致；预览、广告、试看、中间分 P、旧源及无法确认身份的播放不移除。不改变连播/循环。
- 刷新入口：现有刷新提示中的“刷新全部页面”，有播放/未保存输入提示和用户确认。`refreshTabsTask` 保存同隐私上下文的 manifest 支持页面快照；关闭、跳走和丢弃标签跳过。只有观察到原生导航开始才记成功，不自动接受网页离开确认。其他目标完成并返回界面后，单独 ACK 才允许最后处理发起页；部分失败只重试失败目标，成功目标不重复刷新。消息等待与导航确认均有有限期限。
- 链接入口：设置 → Bewly 组件 → 顶栏 → 链接打开行为。`configuredLinkNavigation` 收敛 ALink、顶栏页面入口、用户空间与稍后再看手动跳转；`homeRoute.resolveDefaultAppPage` 同时供实际 outlet 和链接策略使用。显式内部 page、无 page 的默认/激活页和原生首页分别判断；href/target 响应路由，并在指针、键盘、中键和菜单激活时复核。跨源 iframe 使用原生 `_top` 导航，不读取跨源 top.location。结果页顶栏普通搜索原地更新仍优先。
- 两项新设置同步类型、默认值、持久化归一化、导入白名单、设置搜索和四语言；移除相关顶栏入口手写的重复模式分支。未改变视频卡默认打开策略、私信、播放器布局、OLED 或全局玻璃参数。

### 隐私上下文的必要前置

MV3 manifest 使用 `incognito: split`，使网络请求真正使用该普通/无痕上下文的 Cookie，而不只是过滤标签元数据。没有新增主机权限，也没有替用户开启无痕授权。Chrome 明确规定 split 的 Cookie 独立、local/sync 设置存储共享，见[官方说明](https://developer.chrome.com/docs/extensions/reference/manifest/incognito)；共享变更事件参见[固定版本 Chromium 实现](https://chromium.googlesource.com/chromium/src/+/42b022b36244f99067ec9a6aabbcb00d28c7beaf/extensions/browser/api/storage/storage_frontend.cc)。

因此增加仅供四个现有设置消息使用的 `settingsContextRelay`：无痕进程转交原普通进程的设置 patch/读取与云同步入口，不再创建第二个可写协调器或云同步任务。原 epoch/revision、操作 ID、canonical/inFlight/queued 均保留；请求槽有期限、完成清理和后台恢复，不是通用 API 请求框架。TopBar broker 与默认词的会话缓存另按后台上下文分区，防止覆盖对方的缓存记录。

### 四项功能验证与待验收

- 新增 `scripts/verify-four-features.mjs`，由原 `pnpm test` 入口执行，调用实际模块、组件、设置导入函数和后台协调器。覆盖身份解析/去重、跨 await 换号与来源变化、Cookie 先变、停止后已发请求结算、未知结果不重发、明确失败重试、重开进度、worker 重启、状态保存失败、最后分 P/广告/试看/预览/旧媒体、普通/无痕目标隔离、刷新导航确认与 ACK、稳定按钮/焦点/超时、首页路由与修饰键。
- 新增实际设置协调器的普通/无痕并发测试：两个不同字段均保留，revision 为 2；重复原操作 ID 不再递增；临时请求槽清空。没有复制协调器算法自测。
- 7 个候选标签的隔离数据：1 次完整成员读取、2 次写入、1 次批次最终失效；其他项为重复/已有/无法确认。仅报告请求数，不声称 FPS 或内存比例收益。
- Chrome 已重载开发扩展，在独立验收标签读取到新入口及 1 个真实候选；没有确认添加。关闭与重新打开可查看候选。设置搜索可找到两项新设置，自动移除 checkbox 为 false，顶栏/搜索原值仍为“首页外当前页”，下拉含“仅首页使用当前标签”。未修改这些偏好。
- 新弹窗实际截图发现并修正首行被局部顶部模糊遮挡，以及 WatchLater 新增第二根节点导致的页面 Transition 警告。最后一次页面重载后，说明完整可读、计数与按钮正常，未新增该警告；保留现有 Dialog 材质和焦点行为，没有修改全局顶部模糊。
- 未执行真实批量添加、删除、自动移除或用户标签批量刷新；真实多窗口无痕、所有播放器广告/试看版本及网页离开确认组合仍待受控实站验收，当前结果来自隔离模块测试。没有测试移动端。
- 扩展管理页原有 `MAX_WRITE_OPERATIONS_PER_HOUR` 云同步配额错误仍留在错误记录中；未清除记录，也未把它描述为本轮新功能的成功或另行扩大修复范围。
- 最终核验以扩展卡片上的实际重载为准，不能把错误详情页的刷新动作当作重载证据。完成卡片重载后，用户原播放标签的旧提示已原位出现“刷新全部页面”及完整范围/风险提示；未点击它、未刷新原播放标签。开发构建标记仍为同一 watch 进程的 `mu3k292a`。
- 最后独立验收文档还记录一条来自原站 `s1.hdslb.com/.../laputa-home/assets/index-a79b2cf4.js` 的 `Hydration completed but contains mismatches.`；未据此推断扩展根因或宣称控制台完全无日志。此前 WatchLater Transition 警告只有旧时间记录，没有在修正版重载后新增。验收标签已关闭，用户原播放标签及偏好保持。

四项功能最终命令结果：`pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm knip` 最新完整运行退出码均为 0。项目测试共有 **352 条 PASS**；Knip 仍报告原有 **50 / 4 / 4** 个未使用导出/类型/枚举成员提示，未新增这类提示，也未降低规则。复用原 `pnpm dev`，后台与内容脚本最新增量编译成功；没有执行生产 build、打包、改版本、commit 或 push。HEAD 和原 stash 未变。

## 后续：云同步配额、页面 Transition 与图片协议错误

本节更新前述暂未处理的云同步问题。继续在同一工作区修复，不覆盖四项功能及此前体验完善的改动。

| 报错 | 根因与处理 | 主要落点 |
| --- | --- | --- |
| `MAX_WRITE_OPERATIONS_PER_HOUR` | 原 1.5 秒批次与通用短重试没有写频率上限，小时配额耗尽后继续重试。增加持久化的 5 秒批次间隔，覆盖上传及兼容格式竞争恢复最多两次写入；分钟/小时限额分别等待完整窗口并留出余量，冷却跨 worker 重启保留。待上传值仍由原协调器管理，新编辑合并为最新版本。 | `settingsCloudSync.ts`、`settingsCloudSyncWrites.ts` |
| WatchLater 非元素过渡根节点 | 上一阶段新弹窗一度成为页面第二根节点。当前源码已经把弹窗放回已登录页面的唯一根元素中，保留该修复；增加真实组件置于 Vue Transition 下的登录、退出、列表/网格及弹窗测试，没有再套冗余外壳。 | `WatchLater.vue`、`verify-september-adaptation.mjs` |
| HTTP 图片 Mixed Content | 部分图片读取/回退入口保留服务器 HTTP 地址。共享转换仅将 hdslb 图片 CDN 升为 HTTPS，覆盖 LazyPicture 首选格式、失败回退、播放元数据克隆、头像和消息表情/图片；data/blob 及第三方链接不变。私信图片对账同时识别同一资源的 HTTP/HTTPS，发送协议保留原始上传 URL。 | `bilibiliUrl.ts`、`LazyPicture.vue`、`bewlyWidescreen/videoInfo.ts`、Notifications 图片解析及 `privateMessageTransactions.ts` |

- 配额等待使用现有 `blockedByQuota` 状态，设置 → 关于显示保留修改和自动重试时间；普通存储失败继续报告 `failed`。长等待通过一个单次 Chrome alarm 唤醒 MV3，关闭同步清除任务；短批次继续使用现有合并计时器。新增 `alarms` 权限仅用于这个生命周期，没有恢复已移除的 AppAuthScheduler 或 token interval。原 canonical/inFlight/queued、字段版本、未知格式保护与普通/无痕单协调器保持。
- 配额依据：[Chrome storage API](https://developer.chrome.com/docs/extensions/reference/api/storage?hl=zh-cn) 的 120 次/分钟、1800 次/小时写限制；长等待依据 [Chrome alarms](https://developer.chrome.com/docs/extensions/reference/api/alarms)。旧安装已有小时限额错误而尚无调度记录时，首次更新保守等待一个完整窗口；不会通过清空云端或丢弃本地设置恢复。
- 扩展管理页中用户该条 Mixed Content 的完整历史栈指向 `findCommentRoot` 的 `offsetParent` 读取，说明警告发生在其触发布局/资源更新时，并不能证明原始图片由该函数创建。该查找现在先合并重叠选择器；只有多个不同候选时才沿用可见节点优先判定，单一候选不强制刷新整页布局。未注入全局 CSP、改写 Image 原型或增加全站扫描器。
- `scripts/verify-runtime-errors.mjs` 接入原 `pnpm test`，调用真实组件/模块并控制异步响应、存储及时间。覆盖冷却期间编辑和浏览器活动、重启与到期恢复、分钟限额、已有小时错误、普通失败；图片首选/回退/换源、表情、上传对账和原生节点不被改写；评论根重复选择器及多个候选的可见性优先级。
- 同一隔离输入测得：120 次设置修改合并为 **13 次上传周期**，相邻周期至少 5 秒，最后一次写入为最新值；单一评论根经重叠选择器查找 100 次，`offsetParent` 读取 **0 次**，多个根仍选择可见根。没有测量或宣称 FPS、内存提升。

### 本轮实际验证

- `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm knip` 完整运行退出码均为 0；**358 条 PASS**。Knip 维持既有 **50 / 4 / 4** 提示。新增测试最初一处换行规范已修正后重新通过 lint，没有删除行为断言或扩大 ignore。
- 复用已有 `pnpm dev`；后台与内容脚本最新增量编译成功。开发标记仍是该 watch 进程的 `mu3k292a`，另核对 Chrome 加载来源为本项目 `extension/`，从主卡片重载开发扩展后刷新独立验收页。
- 新页面 WatchLater 正常读取 196 项，打开/关闭添加标签弹窗没有新增 Transition 警告；未确认任何批量操作。`BV14ReF6NEWN` 播放器 `readyState=4`，侧栏及原生评论正常挂载；读取当前文档及其 Shadow DOM 未发现 HTTP 图片地址，验收文档没有新增 Mixed Content 警告。
- 实站关于页仍显示 **1 个未同步字段、0 个配额受限字段**，没有显示新的配额错误；不能据此宣布全部设置已上传。未人为耗尽真实浏览器配额，完整小时等待恢复由可控时钟测试覆盖，真实云端最终对账仍待正常同步确认。用户给出的特定 `/bfs/dm/` PNG 在本次实站没有再次出现，不能把当前无警告扩大为所有原生播放器内部资源均已验证。
- 保留扩展管理页历史错误记录；其他扩展的请求错误和 Bilibili 原生 hydration/header 提示不作为本轮扩展报错结论。没有清空记录、发送消息、增删稍后再看或刷新用户其他标签；没有移动端测试、生产 build、打包、commit、push 或版本改动。

## 后续：全站 UI 审查（Emil Design Engineering）

审查开始于 2026-09-16，汇总跨至本地时间 2026-09-17；继续使用本文件作为唯一维护记录。留存 78 张截图：74 张页面/控件状态、1 张尚待确认的占位观察、3 张明确排除的记录。

本轮是审查，没有实施 UI 修复。结论：Bewly 的主体结构、媒体裁切、共享分段控件与原生播放器保留策略已经比较成熟；主要欠缺集中在**文字对比度、鼠标与键盘入口的不对等、旧样式与新 token 的并存，以及少数原生节点移入新容器后的布局适配**。优先修这些基础问题，再调整动效节奏和局部美学。

### 方法、版本与证据边界

- 按用户指定使用 [emil-design-eng](/Users/young/.codex/skills/emil-design-eng/SKILL.md)，结合项目 AGENTS 的圆角、字体、玻璃、间距及交互约束；截图取证使用 Product Design audit 流程。Emil 的通用建议不覆盖本项目明确保留的 Dock 几何展开、液态指示器、私信逐条出现、顶部渐进模糊和原生播放器行为。
- 基线为当前未提交工作区，`message_feature` / `origin/message_feature`，HEAD `c044f261b6ab5c3e68082d3dfd2b9750f2d538f0`。根 AGENTS、package.json、本维护记录及 Git 拓扑已重新核对；没有依据上游旧文件替换本地实现。
- 静态检索覆盖 `src` 中 199 个含 Vue 模板的文件，以及全局样式、原生页面适配样式和播放页生成样式。这里的“覆盖”是全量入口/规则扫描加重点调用链阅读，不声称逐行阅读了所有业务代码。
- 实站为已有登录态的 Chrome 开发扩展。用户确认加载来源是本项目 `extension/` 并从扩展卡片重载后，重新加载独立审查文档，确认 `0.1.1`、`data-dev=true` 和 `mu3k292a`。重载前的 00 截图没有计入审查证据；没有只凭同一 watch 的标记判断脚本更新。
- 主要视口为 **1744×1027 / DPR 2**，另有 **1100×800 / DPR 1** 的桌面检查。通过实际 `innerWidth/innerHeight` 核对了视口；一次没有应用到目标标签的截图 47 已明确排除。没有移动端测试。
- 经用户批准临时检查亮色、普通深色、OLED、原生/Bewly 模式及新关注布局。已恢复“设备 + OLED”、全 Bewly 模式、新关注布局关闭；关注排序原值为“按更新时间”，未修改排序、玻璃强度 14、主题色 `#f43f5e` 或深色基色 `#2a2f2d`。
- 原生页面中的站点内容、账号皮肤、广告拦截提示和其他扩展影响没有自动归因给 Nocturne。没有为了审查关闭其他扩展或清除历史错误。
- 截图与 DOM/CSS 测量均来自本轮。对比度的截图采样值是当前局部背景的测量，不是对所有封面、所有视频帧或所有主题色的保证。未以截图宣称完整 WCAG 合规，也没有做屏幕阅读器朗读验收或 FPS/掉帧测量。
- 完整截图索引：[本轮 UI 证据](/Users/young/.codex/visualizations/2026/09/16/01a0a9d8-0fef-7871-b8ce-c8eef01ba6ba/ui-audit/index.md)。截图仅保存在本机审查目录，没有上传第三方服务。

### 页面与状态覆盖

“实站”表示实际进入、观察并保存截图；“源码”表示检查了现有实现，但没有把它当成真实场景验收。下面按页面职责列出范围，不把同一个共享组件在不同页面出现的次数充作独立问题。

| 步骤 | 页面 / 流程 | 本轮观察与总体状态 | 主要证据 |
| --- | --- | --- | --- |
| 1 | Bewly 首页、个性推荐、订阅、热门、每周必看、排行、直播 | 实站。卡片网格与分段控件总体一致；每周必看、排行存在键盘入口缺口；浅色时间标签对比度不足。 | E01–E07、E58、E66 |
| 2 | 正在关注旧布局、新布局、选择 UP、投稿搜索 | 两种布局实站；新布局对 IGN 投稿执行只读 GTA 查询，侧栏选择和结果归属清楚，目录只有一个主滚动区。新布局检查后恢复关闭。 | E02、E62–E64 |
| 3 | 搜索框、热搜、综合/视频/番剧/影视/用户/直播/专栏结果、筛选、日历 | 七类结果页均进入；番剧/影视/用户在所用查询下为真实空结果。筛选与日历无明显越界，但选中语义、控件高度和空结果恢复提示不足。用户/番剧/影视的有数据详情没有据此全部验收。 | E08–E17、E53 |
| 4 | 收藏、历史、稍后再看、添加播放标签弹窗 | 实站。未执行删除、清空或批量添加。弹窗显示的是已有任务结果，不能作为本轮发送验收。历史/稍后再看在窄窗可用，侧栏封面白字是优先问题。 | E18–E21、E54、E59–E60 |
| 5 | Dock、顶栏历史/收藏/稍后再看浮层、元素设置菜单 | 实站 + 源码。外层圆角和菜单边界总体清楚；键盘与 hover 入口不对等，收藏浮层目录仍是仅点击的 li。 | E22、E74、E76–E77 |
| 6 | 设置：通用、外观、首页、搜索、播放页、视频卡片、Dock、顶栏、Bilibili、关于 | 以上叶子页面实站，并检查搜索定位、Select 方向键、弹窗关闭后的焦点；其他设置叶子复核模板/共享组件。未逐一提交所有设置值或验证全部校验错误。 | E23–E35、E55–E57、E61、E65 |
| 7 | Bewly 动态、评论展开、层级回复 | 实站。评论排序入口、树线与上下文关系可见；未发送评论、转发或点赞。加载阶段的所有收起时序未重测。 | E37、E39 |
| 8 | 私信会话列表与未选中会话空状态 | 实站。列表为实色，初始空状态有操作说明。历史、Composer、表情/图片浮层以及 Reply/At/Love/System 的实站检查受自动已读授权边界限制，见后文；相关代码已审查。 | E42 + Notifications/whisper 源码 |
| 9 | Bewly 番剧、正在追、热播榜、新番时间表、猜你喜欢 | 实站。海报、胶囊、横向溢出提示清楚；海报标题的 20px 层级与普通视频卡不同。日期列属于连续表格，不按独立卡片强加圆角。 | E45、E50–E51、E72 |
| 10 | BV 播放页：沉浸态、控制栏、清晰度菜单、侧栏、原生评论、退出 | 实站。保留原生节点，退出后原生布局恢复；没有更改画质、弹幕设置或执行互动。保存的手动侧栏宽度得到保留。 | E40–E41、E43–E44 |
| 11 | PGC：沉浸态、侧栏、选集、原生模式 | 实站。1100px 窗口中侧栏按 85% 限制宽度；选集的固定原生列数被过大的分配间距拉开。使用键盘 Enter 已确认退出成功，早期未确认的 E52 不算失败证据。 | E46、E48–E49、E75 |
| 12 | 原生首页、原生顶栏、番剧、科技频道 | 经批准切换原生模式后实站，随后恢复 Bewly；原生布局和自有媒体轮廓保留。原生页插件侧栏的键盘焦点仍有半隐藏问题。 | E67–E70 |
| 13 | 原生历史、稍后再看、搜索、动态、用户空间、专栏 | 实站。主题适配生效；原生选中背景与前景没有完全成对适配，原生与覆写页面的几何/密度仍有差异。 | E28、E31、E33、E36、E38、E53 |
| 14 | 创作中心与大会员首页 | 实站只读首屏。没有投稿、领取、续费、支付或改变账号状态；未把营销区原有配色直接定为扩展缺陷。 | E71、E73 |
| 15 | 登录/扫码授权、账号安全、笔记编辑、话题、404、所有网络失败/未登录状态 | 源码审查及覆盖说明。没有为了截图退出账号、触发扫码、制造风控或提交数据；这些状态没有完整实站视觉结论。 | adaptedStyles、AppAuthorizationDialog、Empty、Skeleton 等源码 |
| 16 | 长列表、离屏资源、reduced-motion、快速反向操作 | 检查现有 owner、样式和 reduced-motion 分支；本轮没有重新做长时压力测试、逐帧慢放或所有弹窗反向中断的浏览器测量。 | 各组件/共享动画源码 |

### 按影响排序的发现

优先级：**P1** 为优先恢复可读性；**P2** 为影响可操作性、状态辨识或跨页一致性的明确问题；**P3** 为视觉和操作节奏的改善建议。下表的 After 是建议目标，**不是已经修改后的结果**。

| 编号 | Before（现状） | After（建议目标） | Why（影响与依据） |
| --- | --- | --- | --- |
| U01 · P1 | 收藏、稍后再看封面侧栏固定白色文字，按钮又叠加 35% 白色背景。当前明亮封面上，白字局部对比度约 **1.84:1 / 2.38:1**。 | 在现有侧栏材质 owner 内，让承载文字/操作的区域有受约束的明度；按钮使用可保证对比度的普通实色表面和匹配前景。保留封面气氛，不增加一层模糊。 | 正常可用操作看起来像被禁用；直接影响读按钮和目录。E19/E20/E60；`variables.scss:15`、`CoverSidebarSurface.vue:59`。 |
| U02 · P1 | 浅色视频卡时间标签是 12px、`rgba(58,69,85,.6)`，叠在淡灰 chip 上。抽样合成对比度 **2.91:1**。 | 让 caption/次要元数据在其真实表面上达到项目的 4.5:1 目标；优先调整相应语义前景和 chip 表面，保留 12/16 的层级。 | 时间是内容筛选信息，不能按 disabled 文字处理。不是简单把所有小字加粗。E58；`variables.scss:394`、VideoCardInfo 元数据分支。 |
| U03 · P2 | 原生稍后再看选中项实测约 **3.17:1**；PGC 当前集仍为白字配 `#f43f5e`，约 **3.67:1**。背景随主题变化，部分原生前景仍固定。 | 对实际命中的原生强选中项/主按钮成对设置 `--bew-theme-color / --bew-on-theme-color`；次选项用实色主题 surface 和中性前景，避免只替换原站品牌色变量。 | PGC 的原生子节点没有自动继承 Bewly 控件的前景规则。E31/E49/E53/E75；`variables.scss:622` 及对应原生适配。近白主题色仍需另测。 |
| U04 · P2 | 每周必看期号用可点击 div，排行分类用无 href 的 a，收藏 Pop 目录用可点击 li；没有相应 tabindex/键盘处理。 | 复用现有 Select 或真实 button/选项结构，保留焦点、当前项和 Enter/Space/方向键行为；不重建数据 owner。 | 这些是当前页面主要筛选入口，纯键盘不能完成与鼠标相同的任务。E05/E06/E76；Weekly:275、Ranking:306、FavoritesPop:377。 |
| U05 · P2 | 普通视频卡的“稍后再看”按钮仅在 `isCoverHovered` 为真时挂载。代码虽有 Enter/Space handler，但键盘用户无法先聚焦到未挂载的按钮。 | 卡片获得键盘焦点时也显露同一操作；保持现有鼠标预览和单一稍后再看状态源。 | 可见 hover 效果与可操作入口需要相同覆盖范围。源码确认：`VideoCardCover.vue:379`、`:555`；本轮未用真实添加验证。 |
| U06 · P2 | 搜索设置的 Logo 颜色、分页方式和人物图片使用可点击 div/picture；首页标签和 Dock 配置部分可点击块也缺少键盘入口。 | 用项目已有 SettingsSegmentedControl、真实按钮/单选控件和明确标签表达选择；拖拽外保留可用的键盘操作路径。 | 鼠标可见的选项不应在 Tab 顺序中消失。E25/E26/E30；SearchPage:23、:81、:139，Home:431，DockAndSidebar:185。 |
| U07 · P2 | Dock 内容配置中的多个 checkbox 都被读成“Dock 内容调整 重置”；顶栏不同图标的开关均只叫“显示”；视频卡两个封面比例 slider 共用同名。 | 让控件名称包含对象与行为，如“首页·使用原版”“首页·新标签页”“历史·显示”“双列·封面占比”。 | 共享 SettingsItem 的一个 label ID 被多个子控件复用，视觉近邻没有成为程序关联。E29/E30/E32；SettingsItem:15、Radio:17 及使用点。 |
| U08 · P2 | 顶栏历史等 Pop 由 mouseenter 打开。实际把焦点放到历史链接再按 ArrowDown，浮层数量仍为 0；Enter 则按链接策略导航。 | 为同一入口提供明确的键盘展开方式，区分“打开页面”和“打开菜单”，打开后可移动焦点、Esc 关闭并回到触发项。 | 可到达链接不等于可到达同一浮层的操作。E74 和本轮 DOM 检查；`useTopBarInteraction.ts:146`。 |
| U09 · P2 | 原生页侧栏只根据 hover 展开。1100px 视口中，键盘聚焦“暗色模式”后按钮仍在 x=1080–1120，只有一半在视口内。 | 焦点进入侧栏时完整展开，焦点离开后再服从原收起策略；保留现有鼠标边缘唤出与原生页面布局。 | 焦点环和图标被裁切，剩余可见宽度约 20px。E69；`SideBar.vue:54`、`:278`。未把半隐藏静止态本身误判为 bug。 |
| U10 · P2 | 搜索分类与筛选主要用 class/data-active 表达选择，缺少 aria-pressed/selected；“更多筛选”没有 aria-expanded。 | 按控件真实语义补齐当前值、分组和展开关系；不要仅为了样式套 tab role。 | 视觉上的红色选择无法被辅助技术可靠获知。E09–E17；SearchCategoryTabs:21、SearchVideoFilters:77/:88，相关用户/直播筛选复核同类模式。 |
| U11 · P2 | Select 仅设置 aria-selected，没有独立的可见当前值标记。方向键从“设备”移到“亮色”时，只能看见焦点框，原值“设备”没有视觉标记。 | 分离“已经选中”和“正在聚焦”，例如当前值勾选/实色选中表面，焦点继续保留独立描边。 | 键盘浏览选项时不应丢失原选择。E56；`Select.vue:267`。现有方向键和 Esc 行为可保留。 |
| U12 · P2 | CloseButton surface 已用不透明 elevated 背景，仍附加 backdrop-filter；在 Dialog 内实测按钮 blur(14px)、外层 blur(24px)。 | surface 关闭按钮移除无收益的背景采样，保留几何、阴影和 focus-visible；overlay 变体按自身使用场景处理。 | 违反本项目玻璃层级约束，且没有可见收益。E21 + computed style；`CloseButton.vue:30`。PanelTopBlur 不在本建议内。 |
| U13 · P2 | UnoCSS 后处理把所有 rem 都乘以 15px 的基准字号，旧 `p-4/gap-2/py-1` 实测成为 15/7.5/3.75px；新 token 则是 16/8/4px。 | 明确字体与空间的转换边界，逐步让布局使用 spacing token；先核对生成 CSS 的影响面，不一次性替换全部值。 | 控件同行对齐、内外留白、圆角内缩同时受到影响。E26 测量；`unocss.config.ts:73`。这不是单个页面“差一像素”的问题。 |
| U14 · P2 | `.bewly-wrapper .bew-page-heading { margin:0 }` 覆盖页面上的 mb-6。稍后再看标题 bottom=102，添加按钮 top=102，实际间隔为 0。 | 由标题容器的 gap/padding 管布局间距，或明确标题 margin 的层叠契约，继续复用标题字号样式。 | 页面标题与操作贴在一起，收藏/历史/稍后再看的标题节奏不统一。E20/E60；main.scss:105、WatchLater:446、History:137。 |
| U15 · P2 | 共享 wrapper 设置了行高与字重，却没有设置基础字号；部分未显式声明的设置导航/容器实测继承 16px，而语义 body 是 15px。 | 在明确的插件 UI 根建立 15/24/400 基础，再让导航/控件使用 control/title token；原生站点正文不做无边界全局覆盖。 | 不同模板看似都用“默认字号”，实际却存在额外层级。E26 的 16px 测量；main.scss:96、Settings.vue:875。 |
| U16 · P2 | PGC 原生选集行仍是五个 56px 项目，移入 875px 宽区域后使用 space-between，空隙达到 **148.75px**，下一行又只有两项。 | 保留原生虚拟行分组和业务节点，在允许的样式层让项间距稳定、聚合清楚，或使用其现有列表模式；不得重写虚拟列表行数来凑视觉。 | 序列关系被拉散，鼠标移动距离远，宽侧栏没有转化为有效信息密度。E49 对照 E75；layout.ts:3055，运行时 EpisodeVirtualList_gridRow。 |
| U17 · P2 | reduced-motion 已覆盖 Dock、分段指示器、骨架和部分消息组件，但共享 Button/IconButton/Radio、通用 dropdown、Select 向上菜单、部分 Settings 导航仍缺少对应分支。 | 复用已有 reduced-motion 约定，去掉明显位移、缩放和几何变化，保留必要的轻量状态反馈。先修共享组件，再检查使用点。 | 用户开启减少动态效果后，同一页面会同时出现静止和仍会移动的基础控件。Button:84、IconButton:32、Radio:102、transitionAndTransitionGroup:56、Select:330、Settings:844。源码结论，未切换系统辅助设置实测。 |
| U18 · P3 | 通用向下 dropdown 使用 300ms 的 scale(.96)+位移+blur(2px)，Select 向上是另一套 300ms/blur(4px)。普通 Select surface 没有声明触发点 transform-origin。 | 向上/向下以同一触发关系定义起点与终点，小菜单可先试 150–200ms；为对应方向设置 origin，保持定位 transform 与动画分离。 | 小控件应有即时响应，起源应符合空间关系。Select:247/330、transitionAndTransitionGroup:56；不要把已正确 origin-aware 的顶栏 Pop 一并重写。 |
| U19 · P3 | 顶栏每次 hover 等 320ms，再走 200ms enter，enter 期间 pointer-events:none；邻项没有“已进入浏览模式”的快速切换。Tooltip 则立即开始 300ms 淡入。 | 在现有 hover owner 内区分首次防误触与连续浏览，连续切换缩短等待；Tooltip 统一首次延迟和后续即时反馈。 | 这是高频交互，等待叠加比偶发 Dialog 更容易令人感觉迟钝。useTopBarInteraction:170、popover.scss:160、Tooltip:37。是节奏建议，不是本轮测得的输入延迟基准。 |
| U20 · P3 | 稍后再看侧栏的播放、清空、清除已观看三项同宽、同表面、同字重，远看同等重要。 | 强调播放；清理操作降为次级，并让危险动作有清楚但克制的语义区分。保留原有确认流程。 | 先让用户识别主要任务，再表达破坏性操作。本轮没有点击清空，也没有据此声称缺少确认。E20/E60；WatchLater 侧栏 Button 使用点。 |
| U21 · P3 | 搜索框静止时 46px 高、23px 半径，却使用 superellipse(1.7)，并非完整 round 胶囊；设置搜索的静止态则是 round。 | 如果设计意图是胶囊，静止态使用 round，展开态继续使用语义面板轮廓，并检查转场。维持 550px 最大宽度。 | 仅使用“高度的一半”半径不能抵消 corner-shape 的影响。E01/E08；SearchBar:777/:818/:853。这是形状语义统一建议。 |
| U22 · P3 | 设置外侧导航展开时向右增长并 scale(1.05)，在宽屏截图中覆盖了主面板左侧的文字。 | 在可用空间内优先向外侧空白区展开，或明确压暗/避让内容；同时评估是否需要额外放大。 | 读取分类名称时遮住正在判断的设置内容。E29/E35；Settings:900。保留同一导航实例和既有胶囊→面板设计，不改成两套交叉淡入表面。 |
| U23 · P3 | 历史页视频标题与番剧海报标题使用 20px 层级；常见视频卡标题是 15px。历史的日期在宽屏上还会重复出现。 | 对历史列表优先统一到 title 15/22/600；海报展示是否保留 20px 应作为有说明的展示层级决定，日期按任务需要去重。 | 历史列表的标题更接近区块标题，密度与相邻内容页差异大。E18/E45；History:293、BangumiCard:203、unocss.config:24。尊重用户已有字号偏好。 |
| U24 · P3 | 无搜索结果时只有插图和“暂无数据”，没有解释当前查询/分类，也没有就近恢复操作。 | 给出对应分类的“未找到结果”，提供改短关键词、清除筛选或回到综合的入口；装饰插图补空 alt。 | 用户能分清“查无结果”与“尚未加载”，也能继续任务。E13–E15；Empty:13 及搜索结果页面。不要把 API 错误混成空数据。 |
| U25 · P3 | 1100px 桌面窗口中，稍后再看固定保留侧栏，主区低于已保存的 640px 网格断点，变成约 604×340 的大封面单列。 | 可考虑在这一桌面宽度把概要/批量入口收成上方紧凑工具区，或提供清楚的列表入口；不擅自更改用户已保存的卡片断点。 | 当前行为符合配置，不是越界；但一屏可浏览数量显著下降。E60 对照 E20。作为信息密度取舍讨论，不作为功能回归。 |

### 圆角、层级与美学关系的专门结论

| 关系 | 本轮结论 | 后续验收目标 |
| --- | --- | --- |
| 大面板 → 设置分组 / 内部控件 | modal 24、分组/卡片 12、交互项 8 的层级基本成立。多数问题来自留白和透明度，不是缺少更大的圆角。 | 用实际间距检查嵌套关系，不机械套“父半径减任意像素”；修复 U13 后重新看内缩是否均匀。 |
| 媒体容器 → 图片 / 视频预览 / 遮罩 | 共用媒体裁切与 inherit 的实现应保留；已检查页面没有以双边框作为选中反馈。 | 同一媒体只保留一个裁切 owner，加载和预览切换不得跳角、露底。不能据此宣称所有迟到加载状态已覆盖。 |
| Dock / 头像 / Switch → 平滑矩形 | 真圆和完整胶囊应继续 round；菜单、卡片、Dialog 使用平滑矩形。 | 不把所有元素统一成 superellipse。Dock 的几何中心、指示器和收起展开顺序保持。 |
| 搜索框 → 搜索结果 Pop | 宽度与对齐关系良好，展开后同宽；静止胶囊语义存在 U21 的差异。 | 修形状时同时检查外框、输入框、伪元素和焦点环，不能只改最外一层。 |
| Select / 日期浮层 → 选项 | 外层 12 / 内部 8 的层级可以保留；主要缺口是 U11/U18 的选中标记与动效起点。 | 焦点环完整显示，选中与焦点独立，靠近视口底边时仍保留 8px 安全距离。 |
| 原生 PGC 选集 → Bewly 侧栏 | 外层 24 与原生内部小圆角可以共存；真正突兀的是 U16 的巨大项间距和 U03 的选中前景。 | 不为追求“全部同款”重建原生组件；按实际容器宽度审查内部间距。 |
| 时间表日期列 → 今日高亮 | 这是相邻列组成的连续表格，直角列高亮有结构含义。 | 不为统一卡片风格给每列任意增加圆角，优先保证日期和节目关联。 |
| 独立玻璃 → 内部控件 | 顶栏独立 Pop、搜索框、Dock 的表面范围清楚。CloseButton 的无效重复采样需要修。 | 顶部渐进模糊及 PanelTopBlur 按用户要求保留；实色内部控件不添加 blur，不通过更强模糊掩盖对比度问题。 |
| 原生页面 → 覆写页面 | 原生常见 4–6px 圆角、站点标题与布局密度仍存在；这是适配深度差异，不能一律称为代码错误。 | 为确实由插件管理的表面建立逐类映射；保留原生内容结构、营销插画和业务控件。 |

### 应当保留的设计与交互

- 首页单一搜索实例、550px 宽度上限、分段控件 36/28px 及固定选中字体重量；不因修复局部缺口恢复旧模式入口。
- 主 Dock、头像、Switch 滑块的 round 几何，以及 Dock 在宽屏/1100px 桌面窗口中的居中关系。
- Dialog 的焦点恢复：本轮用 Escape 关闭添加标签弹窗后，焦点确实回到“添加播放标签页”按钮。
- 设置搜索能定位到折叠的“正在关注”配置；Select 已有方向键、Esc 和当前值语义，需补视觉标记而非重写。
- 动态评论现有树线、展开上下文和原生查看链接；不为统一排版拆掉业务 owner。
- BV/PGC 原生播放器和控件保留；两者本轮都确认可以退出 Bewly。保存的侧栏 1184px 手动宽度在窄窗按最大比例约束，没有重置为默认宽度。
- 私信初始空状态没有擅自打开原版 iframe；左侧实色列表、透明详情容器的产品方向保留。详细会话状态仍遵守单独的授权和证据边界。
- 颜色小样存在扩展命中区域的伪元素，不能只看到绘制尺寸小就直接认定命中目标不足。

### 验证建议与实施顺序

1. **先可读性**：U01–U03。使用浅/深/OLED、明亮/暗淡封面以及浅色视频帧实测；检查正文、caption、主按钮、当前集与目录选中项。不要全局简单加粗或加模糊。
2. **再完整入口**：U04–U11。只用键盘走完期号选择、排行分类、卡片稍后再看入口、设置选项、顶栏 Pop、原生页侧栏。打开、切换、取消、恢复焦点都必须可见。
3. **再基础样式**：U12–U17。先确认 rem 转换与标题 margin 的影响范围，再逐模块收敛；在修改后的真实页面检查，无需为纯可逆样式写镜像实现测试。
4. **最后视觉节奏**：U18–U25。小菜单优先短而直接；偶发 Dialog 保留适度入场；需要动态几何的 Dock 和私信继续沿用现有 controller，检查中途反向和 reduced-motion。不要给高频浏览额外加整页 stagger。
5. 后续若实施涉及通知解析、聚合或分页，仍必须通过项目已有通知门禁；本轮没有改变这些逻辑。

### 未覆盖与不能推出的结论

- 消息自动已读的单独授权尚未确认前，本轮只读取会话列表与未选择会话的空状态，没有打开私信历史及 Reply/At/Love/System 分类。对应 Composer、图片/表情菜单、失败/重试/只读会话的结论目前属于源码审查，不能写成实站通过。
- 没有逐一尝试所有设置组合、所有字体、系统字体放大、每个浏览器缩放档位、所有主题色、所有原生版本或所有第三方扩展组合。
- E77 的顶栏稍后再看浮层封面仍为占位：picture 实际有 144×81 的几何尺寸，img 尚未挂载。当前环境存在可能影响页面可见性的扩展，本轮未隔离后台可见性/Observer 与实现原因；该截图不作为封面加载完成的通过证据，也未据此直接归因 HTTP 图片修复失败。
- 没有真实发送、点赞、投币、关注、增删收藏/稍后再看、批量刷新或支付验收；正常页面浏览可能由 Bilibili 自行记录历史，本轮没有清理这些记录来制造“无痕”结果。
- 播放侧栏在明亮画面上的次要文字可读性有风险，但未逐帧量化，不把它等同于 U01 已测得的数值。原生营销轮播的自动动画、原站键盘语义缺口也没有全部归因给插件。
- 此处不是“所有元素、所有状态均已通过”的验收声明。全量 UI 入口与共享规则已纳入审查，实站到达范围和未到达状态如上逐项列出。
- 本轮关于页仍读到 **1 个未同步字段、0 个配额受限字段**；不宣称云同步已全部完成。之前记录的特定原生弹幕 PNG 和真实小时配额恢复也没有因此获得新验收结论。
- 本轮没有改动业务源码，没有生产 build、打包、版本变更、commit、push 或 stash 操作。此前 358 条测试及 lint/typecheck/Knip 结果保留为此前记录，没有冒称本轮重新执行。

### 关键截图

收尾复核：当前文档的主题 class 已恢复 dark + oled-dark，页面模式按钮明确显示“当前为 Bewly Nocturne 配置”；新关注布局已通过设置 checkbox 恢复关闭，主题来源恢复“设备”。临时 1100×800 视口已清除，实测恢复 1744×1027 / DPR 2。本轮创建的两个审查标签及失败导航遗留的空白标签已关闭，用户原有标签保留。Git 的已跟踪文件 diff 仍为接手时的 103 个文件、2898 行增加 / 1378 行删除，暂存区为空；HEAD 与 stash@{0} 完整 SHA 均未变。

实际运行的本轮仓库检查仅有 Git 状态、diff 与 `git diff --check`（退出 0）。该检查不覆盖原本未跟踪的本维护记录；报告和截图索引已另行人工核对。未执行 pnpm lint/typecheck/test/knip，也没有新增或执行 UI 镜像测试。

封面侧栏的白字与半透明按钮（U01/U20）：

![稍后再看侧栏可读性](/Users/young/.codex/visualizations/2026/09/16/01a0a9d8-0fef-7871-b8ce-c8eef01ba6ba/ui-audit/20-watchlater.png)

方向键移动焦点后，Select 当前值没有独立视觉标记（U11）：

![Select 当前值与键盘焦点](/Users/young/.codex/visualizations/2026/09/16/01a0a9d8-0fef-7871-b8ce-c8eef01ba6ba/ui-audit/56-select-focus.png)

原生页侧栏键盘聚焦后仍部分越界（U09）：

![原生页侧栏焦点可见性](/Users/young/.codex/visualizations/2026/09/16/01a0a9d8-0fef-7871-b8ce-c8eef01ba6ba/ui-audit/69-native-sidebar-keyboard.png)

同一原生选集在 Bewly 宽侧栏内被大间距拉散（U16）：

![PGC 选集在 Bewly 侧栏内](/Users/young/.codex/visualizations/2026/09/16/01a0a9d8-0fef-7871-b8ce-c8eef01ba6ba/ui-audit/49-pgc-episodes-1100.png)

原生 PGC 对照：相同五列仍是紧凑排列：

![原生 PGC 五列选集](/Users/young/.codex/visualizations/2026/09/16/01a0a9d8-0fef-7871-b8ce-c8eef01ba6ba/ui-audit/75-native-pgc-1100.png)

## 后续：UI 审查问题修复（2026-09-17）

用户批准实施前述明确问题，并追加搜索点击后的旧页停留、设置滑动条末端漏色、弹幕高级设置顶部空白及圆点被刻度遮挡。继续在 `message_feature` 修改原工作区，未切分支、改版本、commit、push 或操作 stash。采用 emil-design-eng 与 karpathy-guidelines；保留原生业务节点、账号/请求代次、Dock 同一实例几何动画、媒体裁切 owner、OLED、顶栏渐进模糊及 PanelTopBlur。

### 修复与对应关系

| 范围 | Before | After | Why |
| --- | --- | --- | --- |
| 新增 1：搜索导航 | 点击先关闭搜索层，历史写入后才跳转；ISOLATED 的 history 写入又可能未经过 MAIN 路由包装，等待既有 4 秒兜底才更新 outlet。 | 搜索在激活任务中立即提交导航；插件结果页按已提交关键词保存历史。内部导航随后主动调用 **既有 `syncRouteState`**，不新增轮询、事件系统或页面状态。进入结果页取消旧页交叉淡出，异步页面入口立即显示加载态。 | 地址、页面与关键词同步；迟到历史写入不再控制导航。保留结果请求和 URL 恢复的 generation 检查。 |
| 新增 2：滑动条 | padding-box 大小的渐变默认重复绘制到 border-box，右边框露出第二条主题色。 | `Slider` 使用不重复的 background-image，并以 padding-box 裁切；轨道采用 round。 | 消除右端漏色，不改变范围、步长、值归一化或设置写入。 |
| 新增 3：弹幕设置 | 原生 BUI 两页处于同一横向轨道且底部对齐，较矮高级页上方空白；圆点父层 z-index=1，被兄弟刻度层 z-index=2 压住。 | 原轨道顶部对齐，非活动页不贡献高度；保留原生宽度、位移与事件。移除填充条的独立层叠上下文，让原生圆点高于刻度。 | 不克隆/重建弹幕控件，也不修改任何真实弹幕偏好。 |
| U01–U03：对比度 | 封面白字/透明白按钮、浅色 caption、原生选中项前景与背景未配对。 | 封面装饰图采用有上限明度的黑色遮罩，内部操作为普通实色；浅色 `--bew-text-3` 改为实色。原生稍后再看使用 theme surface 的配套前景，PGC 当前集及其标题使用 on-theme 前景。 | 维持文字层级，避免靠加粗、增加模糊或覆盖所有原生子节点补救。 |
| U04–U11：键盘与状态 | 期号/排行/收藏目录/设置卡片缺少键盘入口；稍后再看按钮只在鼠标 hover 挂载；标签语义与控件名称不足。 | 期号保留搜索并接入现有定位 composable，支持方向键、Esc/选择恢复焦点与卸载取消；排行和收藏目录使用 button。设置复用分段控件，首页/Dock 支持局部 Alt+方向键排序；各 checkbox/slider/Select 有具体对象名称。视频卡操作常驻并随卡片焦点显露。搜索分类/筛选补齐 pressed、分组和 expanded；Select 当前值有独立勾选。 | 保留原业务 owner，不依赖不可聚焦的点击容器或全局快捷键。 |
| U08–U09：浮层与侧栏 | 顶栏只能 hover 展开；原生页侧栏键盘聚焦后仍半隐藏。 | 顶栏原 hover controller 接管方向键展开、焦点进入、Esc 返回及离焦关闭；相关监听/计时器随原 scope 释放。侧栏 `focus-within` 立即完整展示。 | 鼠标离开不能关闭正在用键盘操作的浮层，焦点不能留在屏幕外。 |
| U12–U15：基础规则 | CloseButton 实色之上仍采样；rem 空间按 15px 字号缩放；标题 margin 覆盖页面间距；wrapper 隐式继承 16px。 | 移除关闭按钮无效 blur；UnoCSS 把排版和 16px 布局 rem 分离，常用空间映射到共享 token。标题 reset 降低 specificity；稍后再看标题区用容器 gap。插件 wrapper 明确为 15/24/400。 | 保留原生页面正文，不给全站原生 DOM 强制套插件字号。 |
| U16：PGC 选集 | 固定原生行/列被 space-between 拉出巨大间距。 | 只将原虚拟行改为 start + 12px 列间距；原生列数、56px 项宽、虚拟行高度、业务节点保持。 | 序列重新聚合，不通过重建虚拟列表改变原生计算。 |
| U17–U19：动效 | 减少动态效果分支不完整；上下菜单节奏不同；每次顶栏 hover 重复等待。 | Button/IconButton/Radio/Select/搜索筛选/设置导航补齐 reduced-motion。上下 dropdown 共用 150ms、触发侧 origin、独立 translate/scale；键盘打开 Select 不播入场。顶栏相邻浮层立即打开，入场不禁用指针；Tooltip 首次延迟、连续与键盘即时反馈并清理计时器。 | 保留受保护的 Dock/指示器/会话动画及顶部模糊，不引入动画库。 |
| U20–U24：层级与恢复 | 稍后再看操作等权；搜索静止轮廓不是 round；设置导航覆盖正文；历史标题偏大；空搜索缺少恢复入口。 | 播放全部为主按钮，清理为次级并保留确认。搜索外壳/输入/伪元素共用 round→面板轮廓。宽屏设置导航向外展开，窄窗同实例占位让内容避让；历史标题 15/22/600，宽屏隐藏重复日期。搜索空结果说明关键词/分类，提供修改关键词与综合结果入口，装饰图补空 alt。 | 保留内容关联、焦点和单一实例；不把网络错误显示成空结果。 |

U23 的番剧海报 20px 展示层级与 U25 的用户卡片断点属于原审查中的设计取舍，本轮保留，未擅自改写用户字号或 640px 布局偏好。四语言同步新增的期号、键盘排序提示和搜索空结果文案。

### 实际验证

- 完整 `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm knip` 退出码均为 0，测试 **364 条 PASS**。Knip 仍为原有 **50 / 4 / 4** 提示。`git diff --check` 退出 0。测试日志仍包含既有链接夹具触发的 jsdom 未实现整页导航提示；没有据此把它当成实站错误或清空日志。
- 新增 `verify-ui-audit-fixes.mjs` 接入既有测试入口：真实单例路由与 Home outlet 在没有 MAIN 事件时同步变化、后到事件去重、迟到历史不回跳；目标页保存已提交关键词且不把草稿写入历史；顶栏键盘/hover/卸载；期号搜索/选择/Esc；分段控件键盘及独立标签；实际 UnoCSS 生成结果。既有旧断言按新导航/空间契约更新，没有删除账号、请求或原生播放器回归。
- 开始时只读进程检查确认原开发 watch 已结束。启动 `pnpm dev` 相同的 prepare/content/inject/background 开发任务，**跳过 clear**，没有清理正在使用的产物，没有执行生产 build 或打包。watch 保持运行；本轮新标记为 `mu4vztjo`，同一 watch 的增量仍不改变该标记。
- 用户确认重载开发扩展后，在实站读取到 15px wrapper、round 搜索轮廓及新增 `aria-expanded/aria-controls`，不只按 build 标记判定脚本。历史浮层方向键展开并将焦点送入，Esc 回到历史链接；每周必看可搜索 389 期、方向键选中、Enter 提交并回到触发按钮。
- 视频卡只用键盘聚焦稍后再看按钮后，实测 opacity=1、transform=none、焦点环为 solid；未触发添加。Select 方向键聚焦亮色时，设备仍保持 selected 与勾选。
- 浅色时间 chip 的实色前景为 `rgb(82,94,111)`；截图局部背景中值 `rgb(226,227.5,235)`，对比度约 **5.17:1**。PGC 当前集黑字配 `#f43f5e` 约 **5.72:1**。这些是指定状态的实測，不泛化为任意图片、主题色或字体组合。
- 设置滑动条实测 background-repeat=no-repeat、background-clip=padding-box、corner-shape=round，截图右端无第二条主题色。关闭按钮 backdrop-filter=none。
- 弹幕高级页实测活动页与 wrap 的 top 均为 **770px**、高度均为 **223.5px**；原先约 105px 的顶部空白消失。当前可见原生轨道 align-items=flex-start；滑块圆点已位于刻度上方。没有更改弹幕值。PGC 选集实测保留六个 56px 列，gap=12px。
- 稍后再看标题与添加按钮实测 **16px**；收藏/历史标题 margin-bottom=24px；历史标题实测 15/22/600，宽屏重复行内日期 display=none。收藏当前默认封面失效时走正常中性回退，不把该截图当作明亮封面样本；稍后再看实际封面与实色按钮已检查。
- 1744px 设置导航展开后右边界=360、内容左边界=372，间距 **12px**；1100px 桌面窗口中导航右边界=224、内容左边界=232，间距 **8px**。原生页侧栏焦点按钮为 x=1054–1094、宽40px，完整位于1100px视口内。
- 已恢复“设备 + OLED”。临时 1100×800 视口已 reset，实测恢复 **1744×1027 / DPR 2**；主题色 `#f43f5e`、深色基色 `#2a2f2d`、玻璃强度14、页面模式、新关注布局及其他用户偏好未改。
- **搜索最后一次路由补充的实站复核待续**：用户反馈后再次捕获到点击约654ms时 URL 已是 SearchResults、DOM/标题仍是 History；此时不能确认文档加载了刚补充的主动路由同步，因此该结果只留作修复前证据。源码/真实路由模块回归已通过，仍待最后重载后的相同点击检查，不能提前声明此实站问题通过。

尚未实站验证系统 reduced-motion 开关、任意近白主题色、全部原生播放器版本与真实账号写入。没有发送私信/通知已读、点赞、增删收藏/稍后再看、批量刷新。原云同步未同步字段、特定原生弹幕 PNG 与真实小时配额恢复状态沿用前节，不因本次 UI 修复宣称通过。

截图保存在 [本轮修复证据](/Users/young/.codex/visualizations/2026/09/16/01a0a9d8-0fef-7871-b8ce-c8eef01ba6ba/ui-fixes/index.md)，修复前搜索停留证据与修复后的样式截图分别命名。

本次等待重载时的收尾：两张已完成的播放/窄窗验收标签已关闭，仅保留本轮创建的“观看历史”标签作为搜索最终复核入口；用户原有标签未关闭。HEAD 仍为 `c044f261b6ab5c3e68082d3dfd2b9750f2d538f0`，stash@{0} 仍为 `7a6ef9155ccd8e5ce92768702da03b5ae8d7184b`，暂存区为空。新的维护记录与回归脚本仍未提交。

## 后续：批量用户关系查询的扩展上下文失效

用户提供搜索页的 `批量查询用户关系失败: Error: Extension context invalidated.`，堆栈落在 `batchQueryUserRelations`。当前源码调用链为卡片/搜索消费者 → `useUserRelations` → `APIClient` → 共享 `sendMessage`。消息层已经识别并归一化扩展重载失效，但关系查询的 catch 仍将其当作普通失败记录到 console，并保留30秒重试冷却。

- 关系查询复用 `isExtensionContextInvalidatedError`，将这一类错误标为当前内容脚本的终止状态；即使最先报错的是旧查询或旧账号请求，也先处理上下文失效。该状态不随共享关系 owner 的销毁/重建而复位，只有新文档的新内容脚本可以重新查询。
- 通过原有 `requestGeneration` 和 pending 队列结清共用请求的等待者，停止未发送分批，丢弃其他在途请求的迟到结果；保留已确认的关系值，不把失效转换成“未关注”。账号/Cookie、消费者和关系写版本检查保持。
- 普通网络、接口或响应结构失败仍保留原错误反馈及30秒冷却；没有全局屏蔽日志或删除扩展历史错误记录，也没有新增关系 Store、轮询或重试调度器。
- `verify-runtime-errors.mjs` 新增3项回归，执行真实 `APIClient → sendMessage → useUserRelations`，仅替换浏览器 runtime 外部传输。覆盖直接失效和接收端消失、81个MID仅发出首批、已发/未发MID的共用等待者释放、保留确认值、超过冷却仍不重试、重新挂载仍终止、旧账号失效阻断当前与迟到结果，以及普通网络错误可按原冷却恢复。现有关系写冲突、共享查询、Cookie先变、TTL和512预算回归保留。
- 本轮最新完整 `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm knip` 均退出0；**367条 PASS**。Knip维持既有50/4/4提示。复用同一 `pnpm dev` 开发 watch，内容脚本增量编译成功，标记仍为 `mu4vztjo`；没有生产 build、打包、commit、push、版本或 stash 操作。
- 此处为本地真实模块链路的受控失效回归，**尚未执行新补丁下真实浏览器扩展重载的负向验收**。已经失效的旧标签无法被源码修改原位恢复，更新开发扩展后仍需刷新该标签加载新脚本；原错误记录保留。前节搜索跳转补充的最后实站复核也仍待重载后确认，不能因本项367条测试通过而改记为实站通过。

## 后续：提交与推送授权

用户随后明确要求“push所有修改”。提交范围包含本工作区此前全部未提交的体验适配、四项功能、云同步/图片/生命周期修复、UI审查修复、用户关系失效处理、对应回归脚本及本维护记录。继续使用 `message_feature`，唯一推送目标为 `origin/message_feature`；不创建PR、不改版本、不提交忽略的开发产物或仓库外的本机截图，也不处理原stash。提交前重新执行完整lint/typecheck及test/Knip；上述未完成的实站验证边界继续保留，推送不表示这些实站场景已经通过。
